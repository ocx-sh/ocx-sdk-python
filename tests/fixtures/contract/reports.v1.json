{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ocx.sh/schemas/reports/v1.json",
  "$comment": "machine-generated from the CLI's own report types; `reports` maps each --format json root to its definition",
  "reports": {
    "About": {
      "$ref": "#/$defs/About"
    },
    "AnnounceReport": {
      "$ref": "#/$defs/AnnounceReport"
    },
    "AttestationReport": {
      "$ref": "#/$defs/AttestationReport"
    },
    "Catalog": {
      "$ref": "#/$defs/Catalog"
    },
    "Clean": {
      "$ref": "#/$defs/Clean"
    },
    "ConfigSetupData": {
      "$ref": "#/$defs/ConfigSetupData"
    },
    "ConfigTestData": {
      "$ref": "#/$defs/ConfigTestData"
    },
    "ConfigUpdateData": {
      "$ref": "#/$defs/ConfigUpdateData"
    },
    "Dependencies": {
      "$ref": "#/$defs/Dependencies"
    },
    "DependenciesTrace": {
      "$ref": "#/$defs/DependenciesTrace"
    },
    "FlatDependencies": {
      "$ref": "#/$defs/FlatDependencies"
    },
    "EnvVars": {
      "$ref": "#/$defs/EnvVars"
    },
    "CatalogPreview": {
      "$ref": "#/$defs/CatalogPreview"
    },
    "RegenerateReport": {
      "$ref": "#/$defs/RegenerateReport"
    },
    "Installs": {
      "$ref": "#/$defs/Installs"
    },
    "LockReport": {
      "$ref": "#/$defs/LockReport"
    },
    "LoginResult": {
      "$ref": "#/$defs/LoginResult"
    },
    "LogoutResult": {
      "$ref": "#/$defs/LogoutResult"
    },
    "PackageCascadeCheck": {
      "$ref": "#/$defs/PackageCascadeCheck"
    },
    "PackageCascadeRepair": {
      "$ref": "#/$defs/PackageCascadeRepair"
    },
    "CopyReport": {
      "$ref": "#/$defs/CopyReport"
    },
    "PackageDescription": {
      "$ref": "#/$defs/PackageDescription"
    },
    "PackageDescriptions": {
      "$ref": "#/$defs/PackageDescriptions"
    },
    "InspectReport": {
      "$ref": "#/$defs/InspectReport"
    },
    "PackageInspect": {
      "$ref": "#/$defs/PackageInspect"
    },
    "PatchFreezeReport": {
      "$ref": "#/$defs/PatchFreezeReport"
    },
    "PatchPublishReport": {
      "$ref": "#/$defs/PatchPublishReport"
    },
    "PatchSyncReport": {
      "$ref": "#/$defs/PatchSyncReport"
    },
    "PatchTestReport": {
      "$ref": "#/$defs/PatchTestReport"
    },
    "PatchWhyReport": {
      "$ref": "#/$defs/PatchWhyReport"
    },
    "LocatedPaths": {
      "$ref": "#/$defs/LocatedPaths"
    },
    "Paths": {
      "$ref": "#/$defs/Paths"
    },
    "PullDryRun": {
      "$ref": "#/$defs/PullDryRun"
    },
    "PushReport": {
      "$ref": "#/$defs/PushReport"
    },
    "Removed": {
      "$ref": "#/$defs/Removed"
    },
    "SbomListingReport": {
      "$ref": "#/$defs/SbomListingReport"
    },
    "ScriptRunReport": {
      "$ref": "#/$defs/ScriptRunReport"
    },
    "SelfSetupData": {
      "$ref": "#/$defs/SelfSetupData"
    },
    "SelfUpdateData": {
      "$ref": "#/$defs/SelfUpdateData"
    },
    "UpdateCheckData": {
      "$ref": "#/$defs/UpdateCheckData"
    },
    "ShellStateReport": {
      "$ref": "#/$defs/ShellStateReport"
    },
    "VerboseShellState": {
      "$ref": "#/$defs/VerboseShellState"
    },
    "SignatureReport": {
      "$ref": "#/$defs/SignatureReport"
    },
    "StatusReport": {
      "$ref": "#/$defs/StatusReport"
    },
    "SweepReport<SignatureReport>": {
      "$ref": "#/$defs/SweepReport"
    },
    "SweepReport<AttestationReport>": {
      "$ref": "#/$defs/SweepReport2"
    },
    "Tags": {
      "$ref": "#/$defs/Tags"
    },
    "VerificationReport": {
      "$ref": "#/$defs/VerificationReport"
    },
    "VerboseVersionData": {
      "$ref": "#/$defs/VerboseVersionData"
    },
    "VersionData": {
      "$ref": "#/$defs/VersionData"
    },
    "WarmedPaths": {
      "$ref": "#/$defs/WarmedPaths"
    }
  },
  "$defs": {
    "About": {
      "type": "object",
      "properties": {
        "version": {
          "type": "string"
        },
        "registry": {
          "type": "string"
        },
        "platforms": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "libc": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Detected host libc `os.features` tags (e.g. `[\"libc.glibc\"]`,\n`[\"libc.glibc\",\"libc.musl\"]`), empty when none detected (non-Linux,\nNixOS, failed probe). Reflects the same host detection the\nindex-resolution path uses; a host may advertise multiple families."
        },
        "shell": {
          "type": [
            "string",
            "null"
          ]
        },
        "home": {
          "type": "string"
        },
        "channel": {
          "type": [
            "string",
            "null"
          ]
        },
        "commit": {
          "anyOf": [
            {
              "$ref": "#/$defs/CommitInfo"
            },
            {
              "type": "null"
            }
          ]
        },
        "build": {
          "anyOf": [
            {
              "$ref": "#/$defs/BuildInfo"
            },
            {
              "type": "null"
            }
          ]
        },
        "ci": {
          "anyOf": [
            {
              "$ref": "#/$defs/CiInfo"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "version",
        "registry",
        "platforms",
        "libc",
        "shell",
        "home",
        "channel",
        "commit",
        "build",
        "ci"
      ],
      "description": "System information about the ocx installation.\n\nPlain format: colored logo with key-value pairs alongside.\n\nJSON format: flat object with version + registry + platforms + libc +\nshell + home plus optional `channel`, `commit`, `build`, `ci` blocks\nmerged from [`Provenance::current`]. The build-provenance fields are\nabsent on local `cargo build` without git, matching `ocx version\n--format json` behaviour. `libc` is a JSON array of the detected libc\n`os.features` tags (e.g. `[\"libc.glibc\"]`, `[\"libc.glibc\",\"libc.musl\"]`),\nempty when none were detected (non-Linux host, NixOS, or a failed probe)."
    },
    "CommitInfo": {
      "type": "object",
      "properties": {
        "sha": {
          "type": "string",
          "description": "Full 40-character SHA-1."
        },
        "short": {
          "type": "string",
          "description": "8-character abbreviated SHA — convenience for humans."
        },
        "describe": {
          "type": "string",
          "description": "`git describe --tags --dirty` output, including any `-dirty` suffix."
        },
        "dirty": {
          "type": "boolean",
          "description": "`true` if the working tree had uncommitted changes when built."
        },
        "timestamp": {
          "type": [
            "string",
            "null"
          ],
          "description": "ISO-8601 commit timestamp (author date)."
        }
      },
      "required": [
        "sha",
        "short",
        "describe",
        "dirty",
        "timestamp"
      ],
      "description": "Git commit metadata baked at build time."
    },
    "BuildInfo": {
      "type": "object",
      "properties": {
        "timestamp": {
          "type": "string",
          "description": "ISO-8601 UTC build timestamp."
        },
        "profile": {
          "type": "string",
          "description": "`\"release\"` or `\"debug\"` — derived from the `CARGO_DEBUG` flag."
        },
        "target": {
          "type": "string",
          "description": "Target triple the binary was compiled for."
        },
        "rustc": {
          "type": "string",
          "description": "`rustc` version that compiled the binary."
        }
      },
      "required": [
        "timestamp",
        "profile",
        "target",
        "rustc"
      ],
      "description": "Build environment metadata (timestamp, profile, target, rustc)."
    },
    "CiInfo": {
      "type": "object",
      "properties": {
        "provider": {
          "type": "string"
        },
        "run_url": {
          "type": "string",
          "description": "Direct link to the run that produced this binary.\nComposed as `{server_url}/{repository}/actions/runs/{run_id}`."
        },
        "workflow": {
          "type": [
            "string",
            "null"
          ]
        },
        "ref": {
          "type": [
            "string",
            "null"
          ]
        },
        "sha": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "provider",
        "run_url",
        "workflow",
        "ref",
        "sha"
      ],
      "description": "GitHub Actions context. Present only when built under a GitHub\nworkflow that exported the standard `GITHUB_*` env vars."
    },
    "AnnounceReport": {
      "type": "object",
      "properties": {
        "package": {
          "type": "string",
          "description": "The announced `<namespace>/<package>` identifier."
        },
        "status": {
          "type": "string",
          "description": "`\"unchanged\"` when the rebuilt root was byte-identical to the committed\none, so nothing was committed; `\"updated\"` otherwise. An unchanged\n`--fork` run still ensures a pull request when its announce branch is\nahead of the index base."
        },
        "desc_status": {
          "type": "string",
          "description": "`\"updated\"` when the package's `__ocx.desc` artifact moved, so the\nroot's `desc` object was rebuilt and its readme (and logo) written as\nnew content-addressed objects; `\"unchanged\"` when the description sits\nwhere the committed root already recorded it, or there is none."
        },
        "pull_request_url": {
          "type": [
            "string",
            "null"
          ],
          "description": "The opened/updated pull request's web URL."
        },
        "pull_request_number": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0,
          "description": "The opened/updated pull request's number."
        },
        "fork": {
          "type": [
            "string",
            "null"
          ],
          "description": "The verified fork, as `owner/repo`."
        },
        "written_paths": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The relative paths written under the `--out` directory."
        },
        "reserved_tags_dropped": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Tags dropped from the curated set because they are reserved: the\nOCX-internal `__ocx` namespace (which carries the keep tag) and the\nfrozen legacy `<algorithm>.<hex>` keep tags.\nNeither names a version, so announce drops them and reports them here\nrather than failing the run."
        }
      },
      "required": [
        "package",
        "status",
        "desc_status",
        "pull_request_url",
        "pull_request_number",
        "fork",
        "written_paths",
        "reserved_tags_dropped"
      ],
      "description": "Result of a successful `ocx package announce`.\n\nPlain format: a one-row table (`Package`, `Status`, `Pull Request`, `Fork`,\n`Written Paths`) — a dash marks a field the mode did not produce, and\n`Written Paths` is how many were written, not the list (it is unbounded by\nconstruction: one path per tag). Dropped reserved tags have no column; the\ncommand warns about them on stderr when there are any.\n\nJSON format:\n`{ \"package\", \"status\", \"desc_status\", \"pull_request_url\",\n\"pull_request_number\", \"fork\", \"written_paths\", \"reserved_tags_dropped\" }`.\n`status` and `desc_status` are each exactly `\"unchanged\"` or `\"updated\"`;\n`desc_status` is JSON-only, the plain table being at its column budget.\n`pull_request_url` /\n`pull_request_number` / `fork` are always `null` for `--out`; in `--fork`\nmode they are `null` only when the run made no pull request — an unchanged\nrun whose announce branch is ahead of the index base still ensures, and\ntherefore reports, one. `written_paths` is empty outside `--out`.\n`reserved_tags_dropped` is an array, empty rather than absent."
    },
    "AttestationReport": {
      "type": "object",
      "properties": {
        "identifier": {
          "type": "string",
          "description": "User-facing identifier that was attested (echoes the CLI arg)."
        },
        "platform": {
          "type": "string",
          "description": "Platform narrowed into (e.g. `linux/amd64`), or `any` when\n`--platform` was absent and the run attested whatever resolved."
        },
        "subject_digest": {
          "$ref": "#/$defs/Digest",
          "description": "Digest of the subject manifest the Statement names."
        },
        "predicate_type": {
          "type": "string",
          "description": "The resolved `predicateType` URI written into the Statement."
        },
        "bundle_digest": {
          "description": "Digest of the referrer's layer content: the Sigstore bundle blob on a\nsigned attach, the SBOM document itself on an unsigned one. The JSON key\nkeeps its shipped name.",
          "$ref": "#/$defs/Digest"
        },
        "referrer_digest": {
          "description": "Digest of the published OCI referrer manifest wrapping the payload.",
          "$ref": "#/$defs/Digest"
        },
        "sidecar_digest": {
          "description": "Digest of the `sha256-<hex>.att` sidecar manifest, when\n`--signature-format` asked for one.",
          "$ref": "#/$defs/Digest"
        },
        "signed": {
          "type": "boolean",
          "description": "Whether the referrer carries a signature. `false` means the document was\nattached as-is, with no identity behind it — the two certificate fields\nbelow are then absent rather than empty."
        },
        "certificate_identity": {
          "type": "string",
          "description": "Certificate SAN (identity) embedded in the Fulcio cert."
        },
        "certificate_oidc_issuer": {
          "type": "string",
          "description": "Certificate OIDC issuer URL embedded in the Fulcio cert."
        },
        "key_backend": {
          "description": "Which key model produced this attestation (`keyless`, `file`, and —\nonce they exist — `aws_kms` and friends). Absent on an unsigned attach,\nwhere no key model was involved at all.\n\nSpelled as [`SignatureReport`](super::signature::SignatureReport)\nspells it, so one vocabulary describes both commands.",
          "$ref": "#/$defs/KeyBackendKind"
        },
        "public_key_hint": {
          "type": "string",
          "description": "The signing key's cosign hint, in key mode only."
        },
        "transparency_log_index": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0,
          "description": "Whether a transparency record was created, and its log index when so.\n\n**Emitted unconditionally**, `null` included, exactly as the sign report\nemits it: under a key `--rekor-upload` is opt-in, so a missing Rekor\nentry is a legal outcome the operator has to be able to *see* rather\nthan infer from a key that is not there."
        }
      },
      "required": [
        "identifier",
        "platform",
        "subject_digest",
        "predicate_type",
        "signed",
        "transparency_log_index"
      ],
      "description": "Summary of a successful keyless attestation.\n\nPlain format: a single \"Field | Value\" table. `subject_digest` is the answer\n(what was attested) and renders full; `bundle_digest` and `referrer_digest`\nshorten to 12 hex, so one 71-column `sha256:<64hex>` earns its row per view\n(subsystem-cli-api.md \"Plain-Mode Column Budget\"). Both stay full in JSON.\n\nJSON format: `{ identifier, platform, subject_digest, predicate_type,\nbundle_digest, referrer_digest, sidecar_digest, certificate_identity,\ncertificate_oidc_issuer }`.\n\n`bundle_digest` and `referrer_digest` describe the OCI 1.1 referrer and are\nabsent under `--signature-format simplesigning`, which publishes only the\n`sha256-<hex>.att` sidecar `sidecar_digest` names. Their shipped spelling is\nkept: an invocation that does not pass `--signature-format` sees exactly the\nkeys it always did.\n\n`predicate_type` is the **resolved** URI, not the `--type` spelling the\ncaller passed: alias resolution decides what is published, annotated and\nhashed, so echoing it is what keeps the resolution visible rather than\nsurprising (ADR D-c)."
    },
    "Digest": {
      "type": "string",
      "description": "OCI content-addressed digest (e.g. 'sha256:abcdef...').",
      "pattern": "^sha(256|384|512):[0-9a-f]+$"
    },
    "KeyBackendKind": {
      "oneOf": [
        {
          "type": "string",
          "const": "keyless",
          "description": "A Fulcio-issued ephemeral certificate; no long-lived key."
        },
        {
          "type": "string",
          "const": "file",
          "description": "A key read from the filesystem."
        },
        {
          "type": "string",
          "const": "env",
          "description": "A key PEM held in an environment variable."
        },
        {
          "type": "string",
          "const": "awskms",
          "description": "AWS KMS."
        },
        {
          "type": "string",
          "const": "gcpkms",
          "description": "Google Cloud KMS."
        },
        {
          "type": "string",
          "const": "azurekms",
          "description": "Azure Key Vault."
        },
        {
          "type": "string",
          "const": "hashivault",
          "description": "HashiCorp Vault transit."
        },
        {
          "type": "string",
          "const": "k8s",
          "description": "A Kubernetes secret."
        }
      ],
      "description": "What produced or verified a signature — the frozen `signatures[].key_backend`\nvocabulary (`design_spec_cosign_parity.md` §\"--format json\")."
    },
    "Catalog": {
      "type": "object",
      "properties": {
        "repositories": {
          "$ref": "#/$defs/CatalogData"
        }
      },
      "required": [
        "repositories"
      ],
      "description": "Repository catalog listing, optionally including tags per repository.\n\nPlain format: one-column table (Repository) without tags, or two-column\ntable (Repository | Tag) when tags are included.\n\nJSON format: array of repository names without tags, or object keyed by\nrepository name with tag arrays as values."
    },
    "CatalogData": {
      "anyOf": [
        {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      ],
      "description": "Polymorphic catalog payload: either a plain list of repository names or a\nmap of repository names to their tags."
    },
    "Clean": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/CleanEntry"
      }
    },
    "CleanEntry": {
      "type": "object",
      "properties": {
        "kind": {
          "$ref": "#/$defs/CleanKind"
        },
        "dry_run": {
          "type": "boolean"
        },
        "path": {
          "type": "string"
        },
        "held_by": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Project `ocx.lock` paths holding this entry. Empty when the entry is not\nprotected by any registered project, or when `--force` was specified."
        }
      },
      "required": [
        "kind",
        "dry_run",
        "path",
        "held_by"
      ],
      "description": "A single cleaned-up resource entry.\n\nThe `held_by` field lists the absolute paths of every registered project's\n`ocx.lock` that pins this package. Non-empty only for `object` kind entries\nin dry-run mode when the package would have been collected without the\nproject registry. Empty in non-dry-run output (held entries are never\ncollected) and always empty for `temp` and `consent` entries.\n\nSee [`adr_clean_project_backlinks.md`] \"`ocx clean` UX\" for the column\nlayout and JSON shape specification."
    },
    "CleanKind": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "object",
            "temp"
          ]
        },
        {
          "type": "string",
          "const": "consent",
          "description": "A `state/projects/<key>/` directory whose consent stamp was swept.\n\nReported for the same reason the other two are: revoking a project's\nactivation consent is the most consequential thing `ocx clean` does —\nthe project goes inert at the next prompt — and a `--dry-run` that did\nnot name it would be previewing everything except the part a user would\nwant to stop."
        }
      ],
      "description": "The kind of resource cleaned up."
    },
    "ConfigSetupData": {
      "type": "object",
      "properties": {
        "managed_config": {
          "$ref": "#/$defs/ManagedConfigEntry"
        }
      },
      "required": [
        "managed_config"
      ],
      "description": "CLI wrapper around [`ManagedConfigSetupOutcome`] for `ocx config setup`.\n\nPlain format: a key/value table with a `Managed config` summary row.\n\nJSON format: `{\"managed_config\":{\"status\":\"…\"}}`, plus `\"digest\"` on the\nadopt/refresh paths (`adopted` / `already_adopted` / `refreshed` /\n`refresh_unavailable` / `would_refresh`); `refreshed` additionally carries\n`\"previous_digest\"`, `refresh_unavailable` carries `\"reason\"`, both omitted\neverywhere else — the same `managed_config` entry shape `ocx self setup`\nreports, so fleet tooling can parse both with one schema."
    },
    "ManagedConfigEntry": {
      "type": "object",
      "properties": {
        "status": {
          "$ref": "#/$defs/ManagedConfigStatusKind"
        },
        "digest": {
          "type": "string"
        },
        "previous_digest": {
          "type": "string",
          "description": "The digest the snapshot carried before a `refreshed` run."
        },
        "reason": {
          "type": "string",
          "description": "Why a `refresh_unavailable` run could not reach the registry."
        }
      },
      "required": [
        "status"
      ],
      "description": "JSON-serialized managed-config adoption outcome:\n`{\"status\":\"…\"}` or, for the adopt/refresh paths,\n`{\"status\":\"…\",\"digest\":\"sha256:<hex>\"}` (the digest is the operator's\nTOFU signal — always visible on adopt paths). `refreshed` additionally\ncarries `previous_digest`; `refresh_unavailable` carries `reason`. Both are\nomitted everywhere else, so existing consumers stay byte-identical.\n\nShared with `api/data/config_setup.rs` — `ocx config setup` reports the\nsame entry shape so fleet tooling parses both commands with one schema."
    },
    "ManagedConfigStatusKind": {
      "type": "string",
      "enum": [
        "not_configured",
        "already_adopted",
        "adopted",
        "refreshed",
        "refresh_unavailable",
        "cleared",
        "dirty",
        "would_adopt",
        "would_refresh"
      ],
      "description": "Serde-facing mirror of [`ManagedConfigSetupOutcome`] (`snake_case` discriminant)."
    },
    "ConfigTestData": {
      "type": "object",
      "properties": {
        "candidate": {
          "type": "string",
          "description": "The candidate file this report describes."
        },
        "valid": {
          "type": "boolean",
          "description": "Always `true`: an invalid payload fails the command (exit 78) and\nproduces no report. Carried so a JSON consumer reads the verdict from\nthe document rather than inferring it from the exit code."
        },
        "registry_default": {
          "type": [
            "string",
            "null"
          ],
          "description": "Effective `[registry] default`."
        },
        "registries": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Effective `[registries.<name>]` keys, sorted."
        },
        "mirrors": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Effective `[mirrors.\"<host>\"]` keys, sorted."
        },
        "plain_http": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Hosts this machine would contact over plain HTTP once the candidate is\nadopted, sorted — the candidate's own `[registries.<name>] insecure`\nentries unioned with this machine's `OCX_INSECURE_REGISTRIES`, less\nanything the system scope locked shut.\n\nThe gate already computes this set to validate the payload's mirrors;\nreporting it is what makes \"did my entry take effect?\" a one-command\nquestion. A key that is not an exact `host[:port]` grants nothing and\nsimply will not appear here."
        },
        "patches": {
          "anyOf": [
            {
              "$ref": "#/$defs/PatchesView"
            },
            {
              "type": "null"
            }
          ],
          "description": "Effective `[patches]` tier, defaults applied.\n\nSame precedence every ocx invocation uses: the merged config tier when\nit declares `[patches]`, else the forwarded `OCX_PATCHES` env tier. A\ncandidate declaring `[patches]` therefore outranks an ambient\n`OCX_PATCHES`, exactly as it would once adopted."
        },
        "managed": {
          "anyOf": [
            {
              "$ref": "#/$defs/ManagedView"
            },
            {
              "type": "null"
            }
          ],
          "description": "The machine's `[managed]` tier posture — see [`ManagedView`]."
        },
        "unknown_keys": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Dotted paths of keys the config schema ignores, sorted. Advisory: the\nloader ignores unknown keys by design, so these are equally typos and\nsettings a newer ocx understands.\n\nKeys inside a `[mirrors]` entry are NOT covered: that table is parsed\nvalue-first from a raw TOML value, so nothing there is ever ignored by\nthe schema. A misspelled mirror role is silently dropped and reported\nhere as clean."
        }
      },
      "required": [
        "candidate",
        "valid",
        "registry_default",
        "registries",
        "mirrors",
        "plain_http",
        "patches",
        "managed",
        "unknown_keys"
      ],
      "description": "CLI report for `ocx config test` — a candidate managed-config payload\nvalidated locally, plus the configuration adopting it would produce.\n\nPlain format: key/value table; rows with no payload are suppressed, and\nlist-valued fields render one row per value.\n\nJSON format: a fixed shape — every field is always present, with `null` or\n`[]` where a tier is unconfigured, so a consumer can key on\n`.valid`/`.unknown_keys` without probing for the field first."
    },
    "PatchesView": {
      "type": "object",
      "properties": {
        "registry": {
          "type": "string",
          "description": "Registry hosting patch descriptors."
        },
        "path_template": {
          "type": "string",
          "description": "Path template for per-package patch repositories, placeholders intact."
        },
        "required": {
          "type": "boolean",
          "description": "Whether an unavailable companion fails the launch."
        }
      },
      "required": [
        "registry",
        "path_template",
        "required"
      ],
      "description": "The `[patches]` tier as it would resolve after the candidate merges —\ndefaults applied, so an omitted field is reported as the value that would\nactually apply rather than as absent."
    },
    "ManagedView": {
      "type": "object",
      "properties": {
        "source": {
          "type": "string",
          "description": "The effective managed-config source (env override, else the seed)."
        },
        "required": {
          "type": "boolean",
          "description": "Whether an absent or mismatched snapshot fails commands closed."
        }
      },
      "required": [
        "source",
        "required"
      ],
      "description": "The machine's own `[managed]` tier posture.\n\nNever the candidate's: a payload carrying a `[managed]` section is rejected\noutright, so this answers \"which tier would adopt this payload\", not\n\"what does the payload declare\"."
    },
    "ConfigUpdateData": {
      "type": "object",
      "properties": {
        "status": {
          "$ref": "#/$defs/ConfigUpdateStatus"
        },
        "source": {
          "type": "string",
          "description": "The effective managed-config source (flag > env > seed)."
        },
        "digest": {
          "type": "string",
          "description": "The local snapshot's manifest digest."
        },
        "fetched_at": {
          "type": "string",
          "description": "ISO-8601 UTC timestamp of the snapshot's last fetch."
        },
        "policy": {
          "type": "string",
          "description": "The tier's refresh policy (`apply` / `notify` / `manual`)."
        },
        "kill_switches": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Active kill switches (e.g. `OCX_NO_CONFIG_REFRESH`), by env-var name."
        },
        "drift": {
          "type": "boolean",
          "description": "Whether the registry's current digest differs from the local\nsnapshot; present only when reachable (`--check`, online)."
        },
        "tag": {
          "type": "string",
          "description": "The tag the local snapshot was fetched under (snapshot v2 bookkeeping —\nshows which floating/pinned tag the tier tracks)."
        },
        "paused_until": {
          "type": "string",
          "description": "ISO-8601 UTC instant until which the background tick is paused\n(`ocx config update --pause`); absent when no pause is in force."
        },
        "pinned": {
          "type": "string",
          "description": "The version spec pinned alongside an in-force pause\n(`--pause <d> <VERSION>`); absent when the pause carries no pin."
        }
      },
      "required": [
        "status"
      ],
      "description": "CLI report for `ocx config update` — both the full-update path and the\n`--check` probe-only path (mirrors `ocx self update --check`).\n\nPlain format: key/value table; rows with no payload are suppressed.\n\nJSON format:\n`{\"status\":\"…\",\"source\":\"…\",\"digest\":\"…\",\"fetched_at\":\"…\",\"policy\":\"…\",\"kill_switches\":[\"…\"],\"drift\":true}`\n— optional fields present only when the underlying probe produced them.\n\n\nPure data carrier — construct with a struct literal at the call site (no\nstatus derivation lives here; see [`ConfigUpdateStatus`] doc). Fields are\n`pub(crate)` so `command/config_update.rs` builds it directly."
    },
    "ConfigUpdateStatus": {
      "oneOf": [
        {
          "type": "string",
          "const": "not_configured",
          "description": "No managed-config tier is configured."
        },
        {
          "type": "string",
          "const": "already_current",
          "description": "The registry was probed and its digest matches the local snapshot —\nverified current. Reported only when the probe actually ran."
        },
        {
          "type": "string",
          "const": "updated",
          "description": "A new snapshot was fetched and persisted (full-update path only)."
        },
        {
          "type": "string",
          "const": "checked",
          "description": "A probe-only report (`--check`) that ran and detected drift — no swap\nwas attempted."
        },
        {
          "type": "string",
          "const": "check_unavailable",
          "description": "A probe-only report (`--check`) whose registry probe could NOT run —\noffline, no client, source absent, auth failure, or a registry error.\nDistinct from [`Self::AlreadyCurrent`] so operators can tell \"verified\ncurrent\" apart from \"couldn't check\" — the report degrades to\nlocal-state only (source/digest/fetched-at, no live drift)."
        }
      ],
      "description": "Top-level status discriminant for [`ConfigUpdateData`].\n\nSerde serializes each variant to its `snake_case` name. Status derivation\nfrom the underlying probe / update result is an `Implement`-phase concern\n(managed-config phase 4) — this type only carries the already-decided\nvalue."
    },
    "Dependencies": {
      "type": "object",
      "properties": {
        "roots": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Dependency"
          }
        }
      },
      "required": [
        "roots"
      ],
      "description": "Tree view of the dependency graph (default output)."
    },
    "Dependency": {
      "type": "object",
      "properties": {
        "identifier": {
          "$ref": "#/$defs/Identifier"
        },
        "repeated": {
          "type": "boolean"
        },
        "visibility": {
          "anyOf": [
            {
              "$ref": "#/$defs/Visibility"
            },
            {
              "type": "null"
            }
          ]
        },
        "dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Dependency"
          }
        }
      },
      "required": [
        "identifier",
        "repeated",
        "visibility",
        "dependencies"
      ],
      "description": "A node in the dependency tree (for tree view output).\n\n`visibility` is `None` for root nodes (the packages the user asked about)\nand `Some(v)` for dependencies, where `v` is the visibility as declared\nby the parent — not the propagated result."
    },
    "Identifier": {
      "type": "string",
      "description": "OCI identifier in the format 'registry/repository[:tag][@digest]'."
    },
    "Visibility": {
      "type": "string",
      "enum": [
        "sealed",
        "private",
        "public",
        "interface"
      ]
    },
    "DependenciesTrace": {
      "type": "object",
      "properties": {
        "paths": {
          "type": "array",
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/$defs/Identifier"
            }
          }
        },
        "message": {
          "type": "string"
        }
      },
      "required": [
        "paths"
      ],
      "description": "Why view — all paths from roots to a target dependency."
    },
    "FlatDependencies": {
      "type": "object",
      "properties": {
        "entries": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/FlatDependency"
          }
        }
      },
      "required": [
        "entries"
      ],
      "description": "Flat view of the resolved dependency order."
    },
    "FlatDependency": {
      "type": "object",
      "properties": {
        "identifier": {
          "$ref": "#/$defs/Identifier"
        },
        "visibility": {
          "$ref": "#/$defs/Visibility"
        }
      },
      "required": [
        "identifier",
        "visibility"
      ]
    },
    "EnvVars": {
      "type": "object",
      "properties": {
        "entries": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/EnvEntry"
          }
        },
        "binaries": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/BinaryAttribution"
          }
        },
        "entrypoints": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/BinaryAttribution"
          }
        },
        "integrations": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/IntegrationAttribution"
          }
        },
        "advisories": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/LazyAdvisoryReport"
          },
          "description": "Advisories raised for the **deferred** tools in this composition —\nalways present, empty whenever nothing was deferred. Warning-only."
        }
      },
      "required": [
        "entries",
        "binaries",
        "entrypoints",
        "integrations",
        "advisories"
      ],
      "description": "Resolved environment variables for one or more packages, in declaration order.\n\nEach entry carries its [`ModifierKind`] so callers can apply the correct operation:\n- [`ModifierKind::Constant`] — replace any existing value for this key.\n- [`ModifierKind::Path`]     — prepend to any existing value using the platform path separator.\n\nAn ordered list (rather than type-keyed maps) preserves declaration order, allows multiple\nentries per key with different kinds, and naturally accommodates future modifier types.\n\nJSON format: `{\"entries\": [{\"key\": \"...\", \"value\": \"...\", \"type\": \"constant\"|\"path\"|\"list\"[,\n\"separator\": \"...\"][, \"source\": {\"kind\": \"patch\", \"rule\": \"...\", \"companion\": \"...\"}]}, ...],\n\"binaries\": [{\"name\": \"...\", \"package\": \"...\"}, ...], \"entrypoints\": [{\"name\": \"...\", \"package\":\n\"...\"}, ...], \"integrations\": [{\"namespace\": \"...\", \"package\": \"...\", \"payload\": {...}}, ...]}`.\nThe optional `\"separator\"` field is present only for a `\"type\":\"list\"` entry — every other\nkind omits it. The optional `\"source\"` object is present only when `--show-patches` is passed\nand only for companion overlay entries; it is omitted for package-native entries. `binaries`\nand `entrypoints` are the admitted-set claim attribution (`adr_declared_binaries_metadata.md`\n§4) — always present as arrays, possibly empty. `integrations` is the third such array\n(`adr_package_integrations.md` C-014), never collapsed for a single root. The `entries`\nenvelope is the canonical shape shared with `ci export` so consumers can branch on a single\nshape; `binaries`, `entrypoints` and `integrations` are top-level siblings, not nested\ninside `entries`. `advisories` is the fourth such sibling — always present, empty unless a\ndeferred tool raised something; warning-only, and a consumer branches on its `kind`, never\non `message`."
    },
    "EnvEntry": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "value": {
          "type": "string"
        },
        "type": {
          "$ref": "#/$defs/ModifierKind"
        },
        "separator": {
          "type": "string",
          "description": "The separator a [`ModifierKind::List`] entry folds with. `None` on\nevery other kind. Skipped in JSON when `None`."
        },
        "source": {
          "description": "Origin annotation for `--show-patches`. `None` = package native entry;\n`Some(EntrySource::Patch { rule, companion })` = companion overlay entry\ncarrying its provenance. Skipped in JSON when `None`.",
          "$ref": "#/$defs/EntrySource"
        }
      },
      "required": [
        "key",
        "value",
        "type"
      ],
      "description": "A single resolved environment variable entry, tagged with its modifier kind.\n\nThe optional `source` field is populated by the CLI when `--show-patches` is\nenabled. It is `None` for package-native entries and\n`Some(EntrySource::Patch { rule, companion })` for entries that came from a\ncompanion overlay (carrying the rule + companion provenance). The field is\nomitted from JSON output when absent.\n\nThe optional `separator` field carries the fold separator for a\n[`ModifierKind::List`] entry; every other kind omits it. Callers construct\nthis type from entries that already passed compose-time separator\nagreement (`ocx_lib::env::reconcile_list_separators`), so a `list` entry\nreaching here never carries a bare `None` unless nothing in the\ncomposition ever declared one."
    },
    "ModifierKind": {
      "type": "string",
      "enum": [
        "path",
        "constant",
        "list"
      ],
      "description": "How a declared value combines with the variable's existing value."
    },
    "EntrySource": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "package"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "type": "object",
          "properties": {
            "rule": {
              "type": "string"
            },
            "companion": {
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "patch"
            }
          },
          "required": [
            "rule",
            "companion",
            "kind"
          ]
        }
      ],
      "description": "Origin of a resolved environment variable entry.\n\n`Package` is the native origin — the entry came from the package's own\ndeclared metadata; native entries carry `source = None`, so JSON omits the\nfield entirely. `Patch { rule, companion }` is a companion patch overlay\nentry (`--show-patches`) carrying its provenance: `rule` is the descriptor\nrule glob that admitted the companion for the base, and `companion` is the\ncompanion identifier whose interface projection produced the entry.\n\nJSON shape (internally tagged on a `kind` discriminator, lowercase):\n\n```json\n\"source\": { \"kind\": \"patch\", \"rule\": \"<glob>\", \"companion\": \"<companion-id>\" }\n```\n\nThe keys are exactly `kind` (always `\"patch\"` for an overlay entry), `rule`,\nand `companion`. A native entry has no `source` object at all (the field is\nskipped). Pre-1.0 this replaces the Phase-4 `\"source\":\"patch\"` string with\nthe richer provenance object."
    },
    "BinaryAttribution": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "package": {
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "description": "A single admitted `binaries`/`entrypoints` claim, attributed to the\npackage that declared it.\n\n`package` is `Option<String>` — `None` means \"attribution unknown,\" never\n\"this package has zero binaries.\" With the current admission model\n(`ocx_lib::package_manager::composer::compose`'s admitted-set closure),\n`package` is populated for every entry; the `Option` typing leaves room\nfor a future no-clean-attribution source without a breaking schema\nchange. See `adr_declared_binaries_metadata.md` §4 Decision A."
    },
    "IntegrationAttribution": {
      "type": "object",
      "properties": {
        "namespace": {
          "type": "string"
        },
        "package": {
          "type": "string"
        },
        "payload": true
      },
      "required": [
        "namespace",
        "payload"
      ],
      "description": "One admitted integration contribution, attributed to the declaring\npackage.\n\n`payload` is the interpolated payload — arbitrary JSON OCX does not\ninterpret. `package` is `Option` for the same reason\n[`BinaryAttribution::package`] is: `None` means \"attribution unknown\",\nnever \"no payload\".\n\nThe field is `namespace`, not `name`: two of the three sibling arrays are\nname claims that resolve on `PATH`, while an integration row is a keyed\npayload. One row per (package, namespace) pair — an array longer than the\ndistinct-namespace count is the structural guarantee that nothing merged.\nSee `adr_package_integrations.md` C-014."
    },
    "LazyAdvisoryReport": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string"
        },
        "package": {
          "type": "string"
        },
        "key": {
          "type": "string"
        },
        "message": {
          "type": "string"
        }
      },
      "required": [
        "kind",
        "package",
        "message"
      ],
      "description": "One advisory raised while composing a **deferred** tool, in wire shape.\n\n`kind` is the machine discriminator — a consumer branches on it and never\non `message`, which is the human rendering of the same fact. `key` is\npresent only for the two variants that name an environment variable.\n\nAdvisories are warning-only and never fail a compose. They exist because a\ndeferred tool's declared metadata can describe something that will not\nsubstitute cleanly until its content materializes; reaching a log alone\nwould make them unreadable to the tooling this product is a backend for."
    },
    "CatalogPreview": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/CatalogPreviewEntry"
      }
    },
    "CatalogPreviewEntry": {
      "type": "object",
      "properties": {
        "registry": {
          "type": "string"
        },
        "packages": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "registry",
        "packages"
      ],
      "description": "One registry's enumerated package set."
    },
    "RegenerateReport": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/RegenerateEntry"
      }
    },
    "RegenerateEntry": {
      "type": "object",
      "properties": {
        "registry": {
          "type": "string"
        },
        "roots": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "added": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "corrected": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "removed": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "registry",
        "roots",
        "added",
        "corrected",
        "removed"
      ],
      "description": "One registry's drift-repair outcome.\n\n`roots` is what the `p/` walk found, and it is reported even when the three\nlists are empty: a `0` there says the walk found an empty tree, which is a\ndifferent fact from \"the catalog already matched\"."
    },
    "Installs": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/InstallEntry"
      },
      "description": "Installed or selected packages keyed by the user-supplied identifier string.\n\nPlain format: three-column table (Package | Version | Path). `Version`\nrenders the identifier without its digest; the pinned form stays in JSON.\n\nJSON format: object keyed by package identifier, each value an\n`{ identifier, metadata, path }` object."
    },
    "InstallEntry": {
      "type": "object",
      "properties": {
        "identifier": {
          "$ref": "#/$defs/Identifier"
        },
        "metadata": {
          "$ref": "#/$defs/Metadata"
        },
        "path": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "identifier",
        "metadata",
        "path"
      ],
      "description": "A single install or select result entry for CLI output.\n\nThe `path` field holds the symlink that was created or updated (candidate\nfor install, current for select), or `None` when no host symlink was written\n— a foreign-platform install populates the object store but writes neither\nhost pointer (issue #179)."
    },
    "Metadata": {
      "oneOf": [
        {
          "$ref": "#/$defs/Bundle",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "bundle"
            }
          },
          "required": [
            "type"
          ]
        }
      ],
      "description": "OCX package metadata.\n\nDeclares a package's type, extraction options, environment variables, and\ndependencies. Currently only the `bundle` type is supported."
    },
    "Version": {
      "type": "integer",
      "description": "Bundle metadata format version.",
      "enum": [
        1
      ]
    },
    "Env": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Var"
      }
    },
    "Var": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string",
          "description": "The environment variable name (e.g. `PATH`, `JAVA_HOME`)."
        },
        "visibility": {
          "type": "string",
          "enum": [
            "private",
            "public",
            "interface"
          ],
          "description": "Visibility on the entry axis — controls which exec surface (interface vs private)\nsees this entry; see [`crate::package_manager::composer`]. Defaults to\n`private` — publishers explicitly mark contract entries as `public` or\n`interface` to expose them to consumers. `\"sealed\"` is rejected at parse\ntime (ADR Tension 4).",
          "default": "private"
        }
      },
      "required": [
        "key"
      ],
      "oneOf": [
        {
          "$ref": "#/$defs/Path",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "path"
            }
          },
          "required": [
            "type"
          ],
          "description": "A path variable is prepended to any existing value of the environment variable."
        },
        {
          "$ref": "#/$defs/Constant",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "constant"
            }
          },
          "required": [
            "type"
          ],
          "description": "A constant variable replaces any existing value of the environment variable."
        },
        {
          "$ref": "#/$defs/List",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "list"
            }
          },
          "required": [
            "type"
          ],
          "description": "A list variable is appended to any existing value of the environment\nvariable, joined by its own separator, with earlier occurrences of the\nsame contribution removed."
        }
      ],
      "description": "An environment variable declaration.\n\nEach variable has a key (the variable name), a [modifier](Modifier) that\ndetermines how the value is resolved, and a visibility that controls which\nexec surfaces load the entry. The modifier's type and fields are flattened\ninto this object in JSON.\n\n`visibility` defaults to [`Visibility::PRIVATE`] per ADR\n`adr_visibility_two_axis_and_exec_modes.md` Tension 1 (A): publishers must\nopt in explicitly to expose entries on the consumer axis. `\"sealed\"` is\nrejected at parse time — a `Var` invisible on every surface is dead config\n(ADR Tension 4)."
    },
    "Path": {
      "type": "object",
      "properties": {
        "required": {
          "type": "boolean",
          "description": "Whether the resolved path must exist on disk. If `true` and the path is missing, installation fails.\nDefaults to `false`.",
          "default": false
        },
        "value": {
          "type": "string",
          "description": "The value template. `${installPath}` — or its alias `${self.installPath}` — is this package's\ncontent directory, `${deps.NAME.installPath}` a declared dependency's, and `${self.env.KEY}` the\nresolved value of a variable declared earlier in this same list. Append `:native` or `:posix` to\npick the path style. Every other `${...}` is rejected; write `$${` for a literal `${`."
        }
      },
      "required": [
        "value"
      ],
      "description": "A path-type environment variable.\n\nPath variables are prepended to any existing value of the environment variable.\nInterpolation tokens in `value` are replaced at resolution time."
    },
    "Constant": {
      "type": "object",
      "properties": {
        "value": {
          "type": "string",
          "description": "The value template. `${installPath}` — or its alias `${self.installPath}` — is this package's\ncontent directory, `${deps.NAME.installPath}` a declared dependency's, and `${self.env.KEY}` the\nresolved value of a variable declared earlier in this same list. Append `:native` or `:posix` to\npick the path style. Every other `${...}` is rejected; write `$${` for a literal `${`."
        }
      },
      "required": [
        "value"
      ],
      "description": "A constant-type environment variable.\n\nConstant variables replace any existing value of the environment variable.\nInterpolation tokens in `value` are replaced at resolution time."
    },
    "List": {
      "type": "object",
      "properties": {
        "separator": {
          "type": "string",
          "description": "The string joining this contribution to the variable's existing value —\na single space for `JDK_JAVA_OPTIONS`, a comma for `GODEBUG`.\n\nRequired in package metadata, and refused there by `ValidMetadata`\nrather than by serde, so the message names the variable instead of a\nfield offset. Typed as optional because `ocx.toml` and `ocx exec --env`\nmay omit it.\n\nDeliberately no `skip_serializing_if`, unlike the tree's other optional\nwire fields: schemars drops a skipped field from `required`, and the\npublished schema is the write contract where \"a list needs a separator\"\nhas to be enforceable. The only value that would be written as `null` is\none `ValidMetadata` refuses to publish."
        },
        "value": {
          "type": "string",
          "description": "The value template. `${installPath}` — or its alias `${self.installPath}` — is this package's\ncontent directory, `${deps.NAME.installPath}` a declared dependency's, and `${self.env.KEY}` the\nresolved value of a variable declared earlier in this same list. Append `:native` or `:posix` to\npick the path style. Every other `${...}` is rejected; write `$${` for a literal `${`."
        }
      },
      "required": [
        "separator",
        "value"
      ],
      "description": "A list-type environment variable.\n\nList variables are appended to any existing value of the environment\nvariable, with every earlier occurrence of the same contribution removed\nfirst, so re-applying moves the contribution to the back rather than\nduplicating it. The consumer resolving duplicates last-wins is what the\ndirection serves. Interpolation tokens in `value` are replaced at\nresolution time.\n\nThe contribution is opaque: ocx never splits it into elements, so a value\ncarrying the separator is still one contribution."
    },
    "Dependencies2": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Dependency2"
      }
    },
    "Dependency2": {
      "type": "object",
      "properties": {
        "identifier": {
          "$ref": "#/$defs/PinnedIdentifier",
          "description": "Fully qualified pinned OCX identifier with required explicit registry\nand digest. The tag portion is advisory (for update tooling) — only\nthe digest is used for resolution."
        },
        "visibility": {
          "$ref": "#/$defs/Visibility",
          "description": "Controls how this dependency's environment variables propagate.\nDefault: `Sealed` — no env contribution. See [`Visibility`] for the\nfour levels and their semantics.",
          "default": "sealed"
        },
        "name": {
          "anyOf": [
            {
              "$ref": "#/$defs/DependencyName"
            },
            {
              "type": "null"
            }
          ],
          "description": "Optional name for this dependency used in `${deps.NAME.installPath}` interpolation.\n\nWhen set, overrides the default name (last path segment of the OCI repository).\nUse to resolve collisions when two dependencies share the same repository basename."
        }
      },
      "required": [
        "identifier",
        "name"
      ],
      "description": "A pinned dependency descriptor.\n\nThe digest references either an OCI Image Index (for platform-aware\nresolution) or a single manifest (for explicit platform pinning).\nFor cross-compilation, pin the platform-specific manifest digest directly."
    },
    "PinnedIdentifier": {
      "type": "string",
      "description": "Pinned OCI identifier with a required digest and an optional advisory tag: 'registry/repository[:tag]@digest'.",
      "examples": [
        "ocx.sh/cmake:3.28@sha256:43567c07f1a6b07b5e8dc052108c9d4c4a32130e18bcbd8a78c53af3e90325d9",
        "ocx.sh/cmake@sha256:43567c07f1a6b07b5e8dc052108c9d4c4a32130e18bcbd8a78c53af3e90325d9"
      ]
    },
    "DependencyName": {
      "type": "string",
      "description": "Interpolation name for this dependency. Must match ^[a-z0-9][a-z0-9_-]*$ (max 64 chars).",
      "pattern": "^[a-z0-9][a-z0-9_-]*$",
      "maxLength": 64
    },
    "Entrypoints": {
      "type": "object",
      "description": "Map of entrypoint names to entrypoint definitions. Each key is the user-invokable command name; the value object carries an optional `command` field naming the binary the generated launcher dispatches to when it differs from the invokable name (omit it and the name is dispatched directly). An optional `args` array supplies fixed leading arguments the generated launcher prepends before user args; each element may carry `${installPath}` (or its alias `${self.installPath}`), optionally suffixed `:native` or `:posix`, while `${deps.*}` and `${self.env.*}` are not permitted in args and every other `${...}` is rejected — write `$${` for a literal `${`.",
      "additionalProperties": {
        "$ref": "#/$defs/Entrypoint"
      },
      "propertyNames": {
        "pattern": "^[a-z0-9][a-z0-9_-]*$",
        "maxLength": 64
      }
    },
    "Entrypoint": {
      "type": "object",
      "properties": {
        "command": {
          "anyOf": [
            {
              "$ref": "#/$defs/EntrypointName"
            },
            {
              "type": "null"
            }
          ],
          "description": "Dispatch target resolved on the composed `PATH`, when it differs from\nthe invocable name. Absent means the entrypoint name *is* the command\n(the common case): a package may expose `hello` while dispatching a\ndifferently named binary such as `hello-bin`."
        },
        "args": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Fixed leading arguments the generated launcher prepends before the user's\nown arguments. Each element may carry `${installPath}` — or its alias\n`${self.installPath}` — optionally suffixed `:native` or `:posix`; `${deps.*}`\nand `${self.env.*}` are NOT permitted here, and every other `${...}` is rejected\n(write `$${` for a literal `${`). Absent/empty serializes to nothing\n(wire-compatible with the pre-`args` shape)."
        }
      },
      "description": "A single named entrypoint for a package.\n\nThe map key in [`Entrypoints`] supplies the *invocable name* — the\nfilename of the generated launcher. This struct holds the per-entry\nvalue.\n\nThe launcher generated for each entry re-enters via\n`ocx launcher exec '<package-root>' -- <name> [args...]`, preserving\nclean-env execution semantics. `ocx launcher exec` resolves the\n*dispatch command* against the composed `PATH` from the package's `env`\nblock: [`Entrypoint::command`] when set, otherwise the invocable name\nitself (the common case where they coincide).",
      "required": [
        "command"
      ]
    },
    "EntrypointName": {
      "type": "string",
      "description": "Entrypoint name for invocation by users. Must match ^[a-z0-9][a-z0-9_-]*$ and be at most 64 characters.",
      "pattern": "^[a-z0-9][a-z0-9_-]*$",
      "maxLength": 64
    },
    "Binaries": {
      "type": "array",
      "description": "Publisher-declared, unverified claim of interface-surface executable names exposed on PATH by this package. Absent means undeclared; an empty array means the publisher asserts zero interface binaries. On Windows the claim reflects the default executable-resolution set (.exe/.com/.bat/.cmd); a customized child PATHEXT may resolve fewer.",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    },
    "Integrations": {
      "type": "object",
      "additionalProperties": true,
      "description": "A package's declared `integrations` map: namespace key → opaque payload.\n\nKeys iterate in lexicographic order ([`BTreeMap`]), so every derived\nordering — composed rows, cap-violation reporting, closure namespace lists\n— is deterministic across runs and platforms.\n\nAbsent on the wire and empty are the **same** state (deliberately not\n`binaries`' `Option` tri-state): nothing distinguishes \"declares none\" from\n\"did not say\"."
    },
    "Bundle": {
      "type": "object",
      "properties": {
        "version": {
          "$ref": "#/$defs/Version",
          "description": "The version of the bundle metadata format.\nReserved for future schema evolution."
        },
        "strip_components": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint8",
          "minimum": 0,
          "maximum": 255,
          "description": "Number of leading path components to strip when extracting the bundle.\nThis is a convenient feature for archives not created by OCX, which often contain a single top-level directory.\nBy default, OCX will not strip any components, and will extract the archive as-is."
        },
        "env": {
          "$ref": "#/$defs/Env"
        },
        "dependencies": {
          "$ref": "#/$defs/Dependencies2",
          "description": "Ordered list of package dependencies, pinned by digest.\nArray order defines environment import order."
        },
        "entrypoints": {
          "$ref": "#/$defs/Entrypoints",
          "description": "Named entrypoints that `ocx install` generates launchers for.\n\nEach entry produces a Unix `.sh` script and, on Windows, a native\n`.exe` shim plus its `.shim` sidecar under the package's\n`entrypoints/` sibling directory at install time.\nAbsent or empty means no launchers are generated (backward-compat default)."
        },
        "binaries": {
          "anyOf": [
            {
              "$ref": "#/$defs/Binaries"
            },
            {
              "type": "null"
            }
          ],
          "description": "Publisher-declared, unverified claim of interface-surface executable\nnames exposed on `PATH` by this package. `None` means undeclared\n(predates this field); `Some([])` means the publisher asserts zero\ninterface binaries. Deliberately distinct wire states — see\n`adr_declared_binaries_metadata.md` §1. NOT the `Entrypoints`/`Env`/\n`Dependencies` pattern (`X::is_empty` skip) — `None` and empty carry\ndifferent meaning here."
        },
        "integrations": {
          "$ref": "#/$defs/Integrations",
          "description": "Vendor-namespaced configuration blocks for tools OCX does not model.\nKeys are namespaces (reverse-DNS by convention, not enforced); values\nare opaque JSON OCX never interprets, merges, or validates the contents\nof. Absent and empty are the SAME state — the `Entrypoints`/`Env`/\n`Dependencies` skip pattern, deliberately NOT `binaries`' `Option`\ntri-state, because nothing here distinguishes \"declares none\" from \"did\nnot say\"."
        }
      },
      "required": [
        "version",
        "strip_components",
        "binaries"
      ],
      "description": "Bundle package metadata.\n\nDeclares the format version, optional extraction options, environment variables,\ndependencies, and entrypoints that OCX should expose when running commands with\nthis package."
    },
    "LockReport": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/LockEntry"
      },
      "description": "Report emitted by `ocx lock` after a successful resolve.\n\nPlain format: three-column table (Binding | Group | Digest) where the\ndigest column is the host-platform leaf digest.\n\nJSON format: array of `{ binding, group, digest, platforms }` objects,\nwhere `platforms` is the full available-only platform-key → digest map."
    },
    "LockEntry": {
      "type": "object",
      "properties": {
        "binding": {
          "type": "string"
        },
        "group": {
          "type": "string"
        },
        "digest": {
          "type": "string"
        },
        "platforms": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "required": [
        "binding",
        "group",
        "digest",
        "platforms"
      ],
      "description": "A single locked tool entry in the `ocx lock` report.\n\n`binding` is the TOML key in `ocx.toml` (the local binding name);\n`group` is `\"default\"` for entries from the top-level `[tools]`\ntable or the named `[group.*]` key. `digest` is the host-platform\nleaf digest (the primary digest column). `platforms` maps each\nshipped platform's lossless key string to its leaf digest — the full\navailable-only map surfaced in verbose / JSON output."
    },
    "LoginResult": {
      "type": "object",
      "properties": {
        "registry": {
          "type": "string"
        },
        "username": {
          "type": "string"
        }
      },
      "required": [
        "registry",
        "username"
      ],
      "description": "Successful `ocx login` result.\n\nPlain format: nothing on stdout — the human \"Login succeeded\" line is a\nstderr diagnostic emitted via `Api::success`. stdout is the CLI's\nmachine interface and stays empty when there is no parseable payload.\n\nJSON format: `{\"registry\": \"...\", \"username\": \"...\"}` on stdout."
    },
    "LogoutResult": {
      "type": "object",
      "properties": {
        "registry": {
          "type": "string"
        }
      },
      "required": [
        "registry"
      ],
      "description": "Successful `ocx logout` result.\n\nPlain format: nothing on stdout (see [`LoginResult`]).\n\nJSON format: `{\"registry\": \"...\"}` on stdout."
    },
    "PackageCascadeCheck": {
      "type": "object",
      "properties": {
        "reports": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/CascadeReport"
          }
        }
      },
      "required": [
        "reports"
      ],
      "description": "What `cascade check` found, one entry per package in input order.\n\nHolds the library's report values as they are rather than re-modelling\nthem: the finding vocabulary belongs to the algebra that produced it, and a\nsecond copy of the same fields here would be a second thing to keep in\nstep.\n\nPlain format: one row per (tag, platform) slot, plus one row per index\nfinding and per unrepairable item, folded into the same table (they are\nfindings about the same graph, not tables of their own).\n\nJSON format: `{ \"reports\": [...] }` — one library report object per\npackage, each carrying that package's alias states, slot rows, index\nfindings and ignored tags."
    },
    "CascadeReport": {
      "type": "object",
      "properties": {
        "identifier": {
          "$ref": "#/$defs/Identifier",
          "description": "The physical repository the graph was read from."
        },
        "logical": {
          "anyOf": [
            {
              "$ref": "#/$defs/Identifier"
            },
            {
              "type": "null"
            }
          ],
          "description": "The logical name the user asked for, when it differed."
        },
        "aliases": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/AliasState"
          },
          "description": "Per alias, what the registry holds as a whole."
        },
        "rows": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SlotRow"
          },
          "description": "Per-slot rows, sorted by (tag, platform). Includes `Ok` rows so a\nreader sees the whole graph, not only its damage."
        },
        "index_findings": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/IndexFinding"
          },
          "description": "Registry-versus-index findings; always empty for a physical identifier."
        },
        "ignored_tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Tags excluded from the graph, verbatim."
        },
        "unrepairable": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Unrepairable"
          },
          "description": "Findings that need new content published before they can be fixed."
        }
      },
      "required": [
        "identifier",
        "logical",
        "aliases",
        "rows",
        "index_findings",
        "ignored_tags",
        "unrepairable"
      ],
      "description": "The whole finding set for one package — the value both `check` and `repair`\nreport, and the input `plan_repairs` works from."
    },
    "AliasState": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "state": {
              "type": "string",
              "const": "present"
            }
          },
          "required": [
            "state"
          ],
          "description": "An image index — the shape every alias is supposed to have."
        },
        {
          "type": "object",
          "properties": {
            "state": {
              "type": "string",
              "const": "absent"
            }
          },
          "required": [
            "state"
          ],
          "description": "No such tag at the registry: the alias was never created."
        },
        {
          "type": "object",
          "properties": {
            "digest": {
              "$ref": "#/$defs/Digest"
            },
            "state": {
              "type": "string",
              "const": "not-an-index"
            }
          },
          "required": [
            "digest",
            "state"
          ],
          "description": "The tag exists but resolves to a bare image manifest, so it has no\nper-platform slots at all and every expected platform reads as missing."
        }
      ],
      "description": "What the registry holds at an alias tag, taken as a whole."
    },
    "SlotRow": {
      "type": "object",
      "properties": {
        "tag": {
          "$ref": "#/$defs/AliasTag"
        },
        "platform": true,
        "status": {
          "$ref": "#/$defs/SlotStatus"
        },
        "observed": {
          "type": [
            "string",
            "null"
          ],
          "description": "The digest the alias carries for this platform, if any."
        },
        "expected": {
          "type": [
            "string",
            "null"
          ],
          "description": "The digest the fold expects, if any."
        },
        "source": {
          "anyOf": [
            {
              "$ref": "#/$defs/Version"
            },
            {
              "type": "null"
            }
          ],
          "description": "The version the expectation was folded from."
        },
        "observed_source": {
          "anyOf": [
            {
              "$ref": "#/$defs/Version"
            },
            {
              "type": "null"
            }
          ],
          "description": "The version the observed digest belongs to, when the observed content\nis recognisable as some published version's — `None` when the alias\npoints at content no observed leaf carries."
        }
      },
      "required": [
        "tag",
        "platform",
        "status",
        "observed",
        "expected",
        "source",
        "observed_source"
      ],
      "description": "One row of the diff: what one alias holds for one platform versus what the\nfold says it should hold.\n\nDigests are the wire strings verbatim rather than parsed values: an index\nmay legitimately name a digest algorithm this build does not implement, and\na report must be able to show it."
    },
    "AliasTag": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "Root": {
              "type": "object",
              "properties": {
                "variant": {
                  "type": [
                    "string",
                    "null"
                  ]
                }
              },
              "required": [
                "variant"
              ]
            }
          },
          "required": [
            "Root"
          ],
          "additionalProperties": false,
          "description": "The root of one track: `None` is the default track's `latest`, `Some`\nis a variant track's bare name."
        },
        {
          "type": "object",
          "properties": {
            "Version": {
              "$ref": "#/$defs/Version"
            }
          },
          "required": [
            "Version"
          ],
          "additionalProperties": false,
          "description": "Any other alias, named by the version it rolls."
        }
      ],
      "description": "A tag that carries content on behalf of a whole subtree rather than one\nconcrete build.\n\nEvery alias but a track root is named by the [`Version`] it rolls (`3`,\n`3.28`, `3.28.1`). A track root — `latest` for the default track, the bare\nvariant name (`debug`) for a variant track — is named by no version at all,\nwhich is the only reason this enum exists rather than a bare `Version`."
    },
    "SlotStatus": {
      "oneOf": [
        {
          "type": "string",
          "const": "ok",
          "description": "The alias carries exactly the expected entry."
        },
        {
          "type": "string",
          "const": "missing",
          "description": "The alias should carry a platform it does not carry at all."
        },
        {
          "type": "string",
          "const": "stale",
          "description": "The alias carries this platform, but not the expected content."
        },
        {
          "type": "string",
          "const": "orphan",
          "description": "The alias carries a platform nothing folds into it — a leftover from\nan earlier cascade. Reported always; removed only when the entry it\npoints at no longer exists."
        },
        {
          "type": "string",
          "const": "duplicate",
          "description": "The alias carries more than one entry for this platform. Only the last\none resolves, so a shadowed entry is invisible to every consumer while\nstill being published — including when the surviving one is exactly what\nthe fold expects. One row per shadowed entry, beside the row for the\nentry that wins."
        }
      ],
      "description": "The verdict for one (alias, platform) slot."
    },
    "IndexFinding": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "tag": {
              "$ref": "#/$defs/AliasTag"
            },
            "committed": {
              "$ref": "#/$defs/Digest"
            },
            "live": {
              "$ref": "#/$defs/Digest"
            },
            "finding": {
              "type": "string",
              "const": "stale"
            }
          },
          "required": [
            "tag",
            "committed",
            "live",
            "finding"
          ],
          "description": "The index committed a different digest than the alias points at today.\nRepair cannot fix this — announcing the tag can."
        },
        {
          "type": "object",
          "properties": {
            "tag": {
              "$ref": "#/$defs/AliasTag"
            },
            "finding": {
              "type": "string",
              "const": "not-committed"
            }
          },
          "required": [
            "tag",
            "finding"
          ],
          "description": "The registry carries the alias and the index has never recorded it."
        }
      ],
      "description": "A disagreement between the registry graph and the public index that\npublishes it.\n\nOnly ever produced for a logical identifier: a physical repository has no\nreverse mapping back to an index root, so the layer is skipped entirely."
    },
    "Unrepairable": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "tag": {
              "$ref": "#/$defs/AliasTag"
            },
            "digest": {
              "type": "string"
            },
            "reason": {
              "type": "string",
              "const": "child-manifest-missing"
            }
          },
          "required": [
            "tag",
            "digest",
            "reason"
          ],
          "description": "A planned entry points at a child manifest the registry no longer\nholds, so writing the alias would publish a dangling pointer."
        },
        {
          "type": "object",
          "properties": {
            "tag": {
              "$ref": "#/$defs/AliasTag"
            },
            "digest": {
              "type": "string"
            },
            "reason": {
              "type": "string",
              "const": "child-digest-unaddressable"
            }
          },
          "required": [
            "tag",
            "digest",
            "reason"
          ],
          "description": "A planned entry names a digest algorithm this build cannot address, so\nwhether the child is still there could not be checked at all. Distinct\nfrom [`Self::ChildManifestMissing`]: nothing was observed to be gone —\nthe alias is refused because the check could not be made."
        },
        {
          "type": "object",
          "properties": {
            "tag": {
              "$ref": "#/$defs/AliasTag"
            },
            "reason": {
              "type": "string",
              "const": "would-empty-index"
            }
          },
          "required": [
            "tag",
            "reason"
          ],
          "description": "Repairing the alias would leave it with no entries at all. Refused:\nan empty index is worse than a stale one."
        }
      ],
      "description": "Something check found that no repair can fix without new content being\npublished."
    },
    "PackageCascadeRepair": {
      "type": "object",
      "properties": {
        "entries": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/RepairEntry"
          }
        },
        "dry_run": {
          "type": "boolean",
          "description": "True when nothing was written because the run was a preview."
        },
        "announce_tags_path": {
          "type": [
            "string",
            "null"
          ],
          "description": "Where `--announce-tags` was written, when the flag was passed. The\nfollow-up hint can only name the file the user actually asked for."
        }
      },
      "required": [
        "entries",
        "dry_run",
        "announce_tags_path"
      ],
      "description": "What `cascade repair` did, one entry per package in input order.\n\nPlain format: one row per attempted alias write (or, for a preview, per\nplanned write), plus the follow-up announce command when this run moved\ntags or index staleness remains.\n\nJSON format: `{ \"entries\": [...], \"dry_run\", \"announce_tags_path\" }` — one\nper-package entry carrying the finding report, the planned writes and\ntheir outcomes, alongside the run-wide preview flag and the\n`--announce-tags` destination (`null` when the flag was not passed)."
    },
    "RepairEntry": {
      "type": "object",
      "properties": {
        "report": {
          "$ref": "#/$defs/CascadeReport"
        },
        "planned": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/PlannedWrite"
          }
        },
        "outcomes": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/RepairOutcome"
          },
          "description": "Empty for a preview run: nothing was attempted."
        },
        "announce_tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The alias tags this package's run moved or created - the same lines\nwritten to `--announce-tags`, echoed here so a JSON consumer gets them\nwithout reading the file back."
        }
      },
      "required": [
        "report",
        "planned",
        "outcomes",
        "announce_tags"
      ],
      "description": "One package's repair run: what was wrong, what the run planned, and what\nthe registry accepted.\n\n`planned` and `outcomes` are separate because a preview has the first and\nnot the second, and a real run's outcomes can disagree with its plan (an\nalias refused at preflight, a write the registry rejected). Collapsing them\ninto one list would make a refusal indistinguishable from a plan that never\ncovered the alias."
    },
    "PlannedWrite": {
      "type": "object",
      "properties": {
        "tag": {
          "$ref": "#/$defs/AliasTag"
        },
        "index": {
          "description": "The complete index to PUT at `tag`."
        },
        "observed_digest": {
          "anyOf": [
            {
              "$ref": "#/$defs/Digest"
            },
            {
              "type": "null"
            }
          ],
          "description": "The digest the alias points at now — `None` when the alias does not\nexist yet."
        },
        "referenced_digests": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Every child manifest digest `index` references, deduped. Apply\npreflights each one so a missing child refuses the alias instead of\npublishing a dangling pointer."
        },
        "reasons": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SlotRow"
          },
          "description": "The rows that justify the write — what the user is being told changed.\n\nIncludes the alias's [`SlotStatus::Orphan`] rows even though an orphan\nnever causes a write on its own: they are how apply tells a preserved\nleftover from a folded entry when preflight finds a child gone, and so\nwhich entry it may drop instead of refusing the whole alias."
        }
      },
      "required": [
        "tag",
        "index",
        "observed_digest",
        "referenced_digests",
        "reasons"
      ],
      "description": "One alias index to write, recomputed whole.\n\nA repair never patches an alias in place: it rebuilds the entire index from\ncontent the registry already serves and PUTs it, so the write is idempotent\nand a partially-applied run leaves every untouched alias byte-identical."
    },
    "RepairOutcome": {
      "type": "object",
      "properties": {
        "tag": {
          "$ref": "#/$defs/AliasTag"
        },
        "outcome": {
          "$ref": "#/$defs/WriteOutcome"
        }
      },
      "required": [
        "tag",
        "outcome"
      ],
      "description": "What happened to one alias tag in a repair run."
    },
    "WriteOutcome": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "digest": {
              "$ref": "#/$defs/Digest"
            },
            "verified": {
              "type": "boolean"
            },
            "dropped": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "outcome": {
              "type": "string",
              "const": "written"
            }
          },
          "required": [
            "digest",
            "verified",
            "dropped",
            "outcome"
          ],
          "description": "The index was written. `verified` is false when the post-write read-back\nreturned a different digest — a concurrent writer, which is a warning\nrather than a failure: this run's write did land.\n\n`dropped` names the child digests that were removed from the planned\nindex before it went on the wire: dead pointers backing nothing but\norphan slots. Empty for every ordinary write, which is why it is absent\nfrom the JSON rather than an empty array on every row."
        },
        {
          "$ref": "#/$defs/Unrepairable",
          "type": "object",
          "properties": {
            "outcome": {
              "type": "string",
              "const": "refused"
            }
          },
          "required": [
            "outcome"
          ],
          "description": "Refused before any write, because applying it would publish something\nbroken."
        },
        {
          "type": "object",
          "properties": {
            "expected": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Digest"
                },
                {
                  "type": "null"
                }
              ]
            },
            "live": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Digest"
                },
                {
                  "type": "null"
                }
              ]
            },
            "outcome": {
              "type": "string",
              "const": "raced"
            }
          },
          "required": [
            "expected",
            "live",
            "outcome"
          ],
          "description": "The alias moved between the gather this plan was computed from and the\nwrite, so nothing was written. Not a fault and not unrepairable: a\npublish landed in the middle of the run, and re-running the repair\nagainst the new state is the whole fix."
        },
        {
          "type": "object",
          "properties": {
            "message": {
              "type": "string"
            },
            "outcome": {
              "type": "string",
              "const": "failed"
            }
          },
          "required": [
            "message",
            "outcome"
          ],
          "description": "The registry rejected the write. Carries the rendered cause: the\nstructured error is logged once at the boundary, and what a per-tag\nreport row needs is the sentence, not a match target."
        }
      ],
      "description": "The result of attempting one alias index write."
    },
    "CopyReport": {
      "type": "object",
      "properties": {
        "source": {
          "type": "string",
          "description": "The source reference as given."
        },
        "target": {
          "type": "string",
          "description": "The resolved target reference."
        },
        "status": {
          "$ref": "#/$defs/CopyStatus",
          "description": "`copied`, or `planned` under `--dry-run`."
        },
        "platforms": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/CopiedPlatformRow"
          },
          "description": "One row per platform the target offers after this copy, including any it\nalready had that the source does not ship."
        },
        "cascade_tags_written": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Rolling tags written in addition to the target's own tag."
        },
        "keep_tags_written": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Digest-named `__ocx.keep.<algorithm>-<hex>` tags written, one per distinct\nmanifest."
        },
        "referrers_copied": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Referrer manifests carried over — signatures, SBOMs, attestations."
        },
        "sidecars_copied": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "cosign `<algorithm>-<hex>.{sig,att,sbom}` sidecar tags carried over."
        },
        "sidecar_conflicts": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Sidecar tags the target already held under a different manifest, and\nthis copy therefore refused to overwrite. Non-empty means exit 65."
        },
        "blobs": {
          "$ref": "#/$defs/BlobSummary"
        },
        "description": {
          "anyOf": [
            {
              "$ref": "#/$defs/DescriptionOutcome"
            },
            {
              "type": "null"
            }
          ],
          "description": "What became of the repository description, or `null` when\n`--description` was not passed."
        }
      },
      "required": [
        "source",
        "target",
        "status",
        "platforms",
        "cascade_tags_written",
        "keep_tags_written",
        "referrers_copied",
        "sidecars_copied",
        "sidecar_conflicts",
        "blobs",
        "description"
      ],
      "description": "Result of `ocx package copy`.\n\nPlain format: one table, one row per platform — what the target now offers\nand how it got there — and a single stderr status line for the receipt\n(tags written, blob traffic, description). The per-platform breakdown is the\npoint and the only thing on stdout: a promotion that moved one platform into\na target offering three is a legitimate outcome and a serious mistake, and\nonly the row list tells them apart.\n\n**The `Digest` column means two things, and the `Result` column says which.**\nOn an `added`, `replaced` or `unchanged` row it is the digest this copy put\nthere. On a `kept (not in source)` row it is the digest the target already\nhad and this copy never touched.\n\nUnder `--dry-run` the rows read `would add` / `would replace` in plain\noutput only. The JSON `disposition` keeps its stable slug either way, and\nthe top-level `status` is what distinguishes a plan from a promotion.\n\nJSON format:\n`{ \"source\", \"target\", \"status\", \"platforms\": [{ \"platform\", \"digest\",\n\"disposition\" }], \"cascade_tags_written\", \"keep_tags_written\",\n\"referrers_copied\", \"sidecars_copied\", \"sidecar_conflicts\",\n\"blobs\": { \"present\", \"mounted\", \"uploaded\" }, \"description\" }`."
    },
    "CopyStatus": {
      "type": "string",
      "enum": [
        "copied",
        "planned"
      ],
      "description": "Whether this run copied or only planned.\n\nTyped rather than a pre-formatted `String`, so `Serialize` emits a token a\nscript matches on while `Display` and [`CopyStatus::action`] stay free to\nread like English (`subsystem-cli-api.md`, \"Typed Enums Over Strings\")."
    },
    "CopiedPlatformRow": {
      "type": "object",
      "properties": {
        "platform": {
          "type": "string"
        },
        "digest": {
          "type": "string",
          "description": "The leaf digest the target serves for this platform. For a\n`kept-not-in-source` row this is the digest it already had."
        },
        "disposition": {
          "$ref": "#/$defs/Disposition",
          "description": "Typed, so JSON carries `added` / `unchanged` / `replaced` /\n`kept-not-in-source` while the table renders the prose."
        }
      },
      "required": [
        "platform",
        "digest",
        "disposition"
      ],
      "description": "What became of one platform."
    },
    "Disposition": {
      "oneOf": [
        {
          "type": "string",
          "const": "added",
          "description": "The target's index had no entry for this platform."
        },
        {
          "type": "string",
          "const": "unchanged",
          "description": "The target already pointed at this exact digest — nothing to do."
        },
        {
          "type": "string",
          "const": "replaced",
          "description": "The target pointed at a different digest for this platform."
        },
        {
          "type": "string",
          "const": "kept-not-in-source",
          "description": "The target offers this platform and the source does not, so the merge\nleaves it alone. Reported because a filtered copy that silently left a\nmixed index behind would be indistinguishable from a complete one."
        }
      ],
      "description": "What became of one platform at the target.\n\nTwo renderings, one vocabulary. `Serialize` is the machine-readable form a\n`--format json` consumer matches on; `Display` below is the terminal prose,\nwhich is free to read like English precisely because nothing parses it.\nPre-formatting the prose into a JSON string made a sentence with a space and\ntwo parentheses into a wire value (`subsystem-cli-api.md`, \"Typed Enums Over\nStrings\")."
    },
    "BlobSummary": {
      "type": "object",
      "properties": {
        "present": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Already at the target — nothing transferred."
        },
        "mounted": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Mounted from another repository in the same registry."
        },
        "uploaded": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Downloaded from the source and uploaded to the target."
        }
      },
      "required": [
        "present",
        "mounted",
        "uploaded"
      ],
      "description": "Blob traffic, summed over every platform."
    },
    "DescriptionOutcome": {
      "oneOf": [
        {
          "type": "string",
          "const": "copied",
          "description": "Pulled from the source and pushed to the target."
        },
        {
          "type": "string",
          "const": "absent",
          "description": "The source publishes none, so there was nothing to copy. Not a failure:\na description is repository-level prose and legitimately absent."
        },
        {
          "type": "string",
          "const": "skipped-dry-run",
          "description": "`--dry-run` was in force, so the description was not read or written."
        }
      ],
      "description": "What became of the repository description.\n\nReported rather than left to a stderr warning: `--format json` is how a CI\njob finds out whether the catalog page travelled, and a warning is not a\nfield. `None` — the flag was not passed — serializes as `null`, so the key\nis always there to branch on."
    },
    "PackageDescription": {
      "anyOf": [
        {
          "$ref": "#/$defs/Inner"
        },
        {
          "type": "null"
        }
      ]
    },
    "Inner": {
      "type": "object",
      "properties": {
        "title": {
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "keywords": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "description": "A single field in the description.",
      "required": [
        "title",
        "description",
        "keywords"
      ]
    },
    "PackageDescriptions": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/PackageDescription"
      }
    },
    "InspectReport": {
      "type": "object",
      "properties": {
        "platform": {
          "type": "string"
        },
        "packages": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/PackageInspect"
          }
        },
        "env": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/EnvEntry"
          }
        }
      },
      "required": [
        "packages",
        "env"
      ]
    },
    "PackageInspect": {
      "type": "object",
      "$comment": "`name` and `identifier` are always written. `pinned_identifier` and `pinned_digest` appear together, and only when the body pinned an artifact. The remaining keys come from exactly one body shape: `candidates` (a candidate listing or an `ocx.lock` binding), `metadata` + `layers` (a manifest), or `platform` + `metadata` + `layers` + `resolution` (a resolved package). `closure` rides along with the last two under `--closure`.",
      "properties": {
        "name": {
          "type": "string"
        },
        "identifier": {
          "$ref": "#/$defs/Identifier"
        },
        "pinned_identifier": {
          "type": "string"
        },
        "pinned_digest": {
          "type": "string"
        },
        "candidates": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/CandidateOut"
          }
        },
        "platform": {
          "$ref": "#/$defs/Platform"
        },
        "metadata": {
          "$ref": "#/$defs/Metadata"
        },
        "layers": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Layer"
          }
        },
        "resolution": {
          "$ref": "#/$defs/Resolution"
        },
        "closure": {
          "$ref": "#/$defs/ClosureOut"
        }
      },
      "required": [
        "name",
        "identifier"
      ]
    },
    "CandidateOut": {
      "type": "object",
      "properties": {
        "digest": {
          "type": "string"
        },
        "pinned": {
          "type": "string",
          "description": "This candidate as a pullable reference — the entry's identifier with\nthis child's digest attached. Emitted for the same reason the entry\ncarries `pinned_identifier`: splicing one by hand means knowing where\nthe tag goes relative to the digest.\n\nNamed `pinned` rather than `pinned_identifier` because a candidate has\nexactly one digest and so nothing to disambiguate against — the same\nreason `resolution.pinned` is spelled that way."
        },
        "platform": {
          "type": "string"
        },
        "media_type": {
          "type": "string",
          "description": "Absent for a lock-projected candidate: `ocx.lock` records the leaf\ndigest per platform, not the descriptor that pointed at it."
        },
        "size": {
          "type": "integer",
          "format": "int64"
        }
      },
      "required": [
        "digest",
        "pinned",
        "platform"
      ],
      "description": "One platform child of an image index, or one locked platform leaf."
    },
    "Platform": {
      "type": "object",
      "description": "An OCI image-spec platform object. `Platform::Any` writes os and architecture as \"any\".",
      "properties": {
        "architecture": {
          "type": "string"
        },
        "os": {
          "type": "string"
        },
        "os.version": {
          "type": "string"
        },
        "os.features": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "variant": {
          "type": "string"
        },
        "features": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "architecture",
        "os"
      ]
    },
    "Layer": {
      "type": "object",
      "properties": {
        "digest": {
          "type": "string"
        },
        "media_type": {
          "type": "string"
        },
        "size": {
          "type": "integer",
          "format": "int64"
        }
      },
      "required": [
        "digest",
        "media_type",
        "size"
      ],
      "description": "A single layer descriptor from the inspected manifest (default mode) or the\nplatform-selected manifest (`--resolve`)."
    },
    "Resolution": {
      "type": "object",
      "properties": {
        "pinned": {
          "type": "string"
        },
        "chain": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ChainOut"
          }
        }
      },
      "required": [
        "pinned",
        "chain"
      ],
      "description": "The OCI resolution chain for the selected platform. Carries only the walk\n(`index` → `manifest` → `config`); the platform-selected manifest's layers\nare rendered alongside the metadata, not inside the chain."
    },
    "ChainOut": {
      "type": "object",
      "properties": {
        "digest": {
          "type": "string"
        },
        "role": {
          "type": "string"
        },
        "media_type": {
          "type": "string"
        },
        "size": {
          "type": "integer",
          "format": "int64"
        }
      },
      "required": [
        "digest",
        "role",
        "media_type",
        "size"
      ],
      "description": "One blob in the resolution chain. Same descriptor surface as a layer\n(digest, media type, size) plus the OCI `role` so a consumer can tell\nthe index from the manifest from the config without decoding digests."
    },
    "ClosureOut": {
      "type": "object",
      "properties": {
        "deps": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ClosureDepOut"
          },
          "description": "Transitive dependencies in transitive-closure order (deps before\ndependents). The inspected root is NOT listed here — it is named by the\ntop-level `identifier` and appears in each surface's attributions."
        },
        "surface": {
          "$ref": "#/$defs/SurfacesOut"
        },
        "conflicts": {
          "$ref": "#/$defs/ConflictsOut"
        }
      },
      "required": [
        "deps",
        "surface",
        "conflicts"
      ],
      "description": "The dependency closure emitted with `--closure`. Everything nests under one\nobject: `deps` (the transitive dependencies in transitive-closure order),\n`surface` (the interface + private projections), and interface-projection\n`conflicts`."
    },
    "ClosureDepOut": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Short display name — the repository's final path segment (e.g.\n`deps-mid`). The flat plain tree labels each dep by this."
        },
        "identifier": {
          "type": "string",
          "description": "Always digest-pinned — a closure node is a resolved artifact, never a\ntag. There is no separate `digest` key because this one already ends in\nit."
        },
        "effective_visibility": {
          "type": "string",
          "description": "Composed-from-root visibility — always present (the root, whose axis is\nundefined, is excluded from `deps`)."
        },
        "binaries": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Tri-state, mirrors `Bundle.binaries`: key absent = undeclared,\n`Some(empty)` = publisher asserts zero interface executables."
        },
        "entrypoints": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "integrations": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The dep's own declared integration namespace keys, lexicographically\nordered. Keys only — a closure node is not installed, so\n`${installPath}` has no value and an interpolated payload would be a\nhalf-truth. Always present, `[]` when the dep declares none: absent and\nempty are the same state here (unlike `binaries`' tri-state)."
        },
        "dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ClosureEdgeOut"
          },
          "description": "The dep's own declared dependency edges (as authored) — lets a consumer\nrebuild the DAG from the flat list."
        }
      },
      "required": [
        "name",
        "identifier",
        "effective_visibility",
        "entrypoints",
        "integrations",
        "dependencies"
      ],
      "description": "One transitive dependency of a [`ClosureOut`], in transitive-closure order."
    },
    "ClosureEdgeOut": {
      "type": "object",
      "properties": {
        "identifier": {
          "type": "string"
        },
        "visibility": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "identifier",
        "visibility",
        "name"
      ],
      "description": "A declared dependency edge (as authored) inside a [`ClosureDepOut`]."
    },
    "SurfacesOut": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/$defs/SurfaceOut",
          "description": "Consumer-facing: what reaches someone installing the root."
        },
        "private": {
          "$ref": "#/$defs/SurfaceOut",
          "description": "Internal: what is visible on the package's own private axis. Public\nentries appear in both surfaces (public crosses both axes)."
        }
      },
      "required": [
        "interface",
        "private"
      ],
      "description": "The two symmetric surface projections of a closure — \"what binaries /\nentrypoints / env keys would land, and on which axis, if this were\ninstalled\", without installing."
    },
    "SurfaceOut": {
      "type": "object",
      "properties": {
        "binaries": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/BinaryAttribution"
          }
        },
        "entrypoints": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/BinaryAttribution"
          }
        },
        "env": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/EnvVarAttribution"
          },
          "description": "Env keys each admitted node exposes on this axis, attributed to the\ndeclaring package. Values are omitted — they are `${installPath}`-\ntemplated and only concrete after install."
        },
        "integrations": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/NamespaceAttribution"
          },
          "description": "Integration namespace keys each admitted node declares, attributed to\nthe declaring package. Payload-free — a closure node is not installed,\nso `${installPath}` has no value and the payload would be a half-truth,\nthe same reason [`SurfaceOut::env`] omits values."
        },
        "binaries_complete": {
          "type": "boolean",
          "description": "`false` iff any admitted node has undeclared `binaries`\n(\"couldn't determine \\u{2260} determined zero\"). Entrypoints have no\nsuch flag — the entrypoint map keys are always authoritative."
        }
      },
      "required": [
        "binaries",
        "entrypoints",
        "env",
        "integrations",
        "binaries_complete"
      ],
      "description": "One projected surface — binaries/entrypoints/env/integrations admitted on\na single axis."
    },
    "EnvVarAttribution": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "type": {
          "type": "string"
        },
        "separator": {
          "type": "string",
          "description": "The declared separator for a `list`-kind entry; `None` for every other\nkind. Skipped in JSON when `None`."
        },
        "package": {
          "type": "string"
        }
      },
      "required": [
        "key",
        "type"
      ],
      "description": "One env key exposed on the interface surface, attributed to the package that\ndeclares it. `type` is the modifier kind (`path` | `constant` | `list`).\nNo value — see [`SurfaceOut::env`]."
    },
    "NamespaceAttribution": {
      "type": "object",
      "properties": {
        "namespace": {
          "type": "string"
        },
        "package": {
          "type": "string"
        }
      },
      "required": [
        "namespace"
      ],
      "description": "One integration namespace a closure node declares, attributed to the\ndeclaring package. The field is `namespace` — the same word the flat\n`ocx env` / `ocx package env` envelope uses (`IntegrationAttribution`), so\none concept has one key spelling across both surfaces\n(`adr_package_integrations.md` D16).\n\nNo `value`: the closure envelope is payload-free — see [`SurfaceOut::env`]\nfor the same reason applied to env values."
    },
    "ConflictsOut": {
      "type": "object",
      "properties": {
        "entrypoints": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/EntrypointConflictOut"
          }
        },
        "repositories": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/RepositoryConflictOut"
          }
        }
      },
      "required": [
        "entrypoints",
        "repositories"
      ],
      "description": "Install/compose-gate conditions detected over the interface projection\n(Codex C2). Both arrays always present; empty means the surface is\nrealizable. Inspect stays a view, not a gate — exit 0 either way."
    },
    "EntrypointConflictOut": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "packages": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "name",
        "packages"
      ],
      "description": "Two or more interface-admitted closure nodes declare the same entrypoint\nname."
    },
    "RepositoryConflictOut": {
      "type": "object",
      "properties": {
        "repository": {
          "type": "string"
        },
        "digests": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "repository",
        "digests"
      ],
      "description": "One repository resolved to two or more distinct digests on the interface\nprojection."
    },
    "PatchFreezeReport": {
      "type": "object",
      "properties": {
        "companions": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of companion packages pinned by the snapshot."
        },
        "descriptors": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of descriptor blobs pinned by the snapshot."
        },
        "path": {
          "type": "string",
          "description": "Absolute path of the written `patches.snapshot.json` file."
        }
      },
      "required": [
        "companions",
        "descriptors",
        "path"
      ],
      "description": "Report emitted by `ocx [--global] patch freeze`.\n\nPlain format: two-column table (`Kind | Count`) summarising how many\ncompanions and descriptors were frozen, followed by the snapshot path.\n\nJSON format: `{ \"companions\": N, \"descriptors\": N, \"path\": \"...\" }`."
    },
    "PatchPublishReport": {
      "type": "object",
      "properties": {
        "reference": {
          "type": "string",
          "description": "Canonical reference the descriptor was published to\n(`registry/repository:__ocx.patch`)."
        },
        "manifest_digest": {
          "type": "string",
          "description": "Manifest digest of the pushed `__ocx.patch` artifact."
        },
        "rules": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of rules in the published descriptor."
        }
      },
      "required": [
        "reference",
        "manifest_digest",
        "rules"
      ],
      "description": "Report emitted by `ocx [--global] patch publish`.\n\nPlain format: a single-row table (`Reference | Digest | Rules`) showing the\npublished patch repo reference, the manifest digest, and the descriptor rule\ncount.\n\nJSON format: `{ \"reference\": \"...\", \"manifest_digest\": \"...\", \"rules\": N }`."
    },
    "PatchSyncReport": {
      "type": "object",
      "properties": {
        "bases_checked": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of installed bases (plus global root) that were checked."
        },
        "descriptors_updated": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of descriptor blobs where the upstream digest advanced."
        },
        "companions_installed": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of companion packages installed or re-installed."
        }
      },
      "required": [
        "bases_checked",
        "descriptors_updated",
        "companions_installed"
      ],
      "description": "Report emitted by `ocx [--global] patch sync`.\n\nPlain format: a single-row summary table (`Checked | Updated | Companions`)\nshowing how many bases were checked, descriptors updated, and companions\ninstalled.\n\nJSON format: `{ \"bases_checked\": N, \"descriptors_updated\": N, \"companions_installed\": N }`."
    },
    "PatchTestReport": {
      "type": "object",
      "properties": {
        "base": {
          "type": "string",
          "description": "The base identifier the descriptor was composed onto."
        },
        "companions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Companion identifiers that matched the base under the descriptor's rules."
        },
        "entries": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/PatchTestEntry"
          },
          "description": "Composed environment entries (base interface surface + companion overlay)."
        }
      },
      "required": [
        "base",
        "companions",
        "entries"
      ],
      "description": "Report emitted by `ocx patch test` (env-inspection mode, no script/command).\n\nShows which companions the descriptor matched for the base identifier and\nthe composed environment that results from overlaying them.\n\nPlain format: a hint line naming the matched companions, followed by a table\nof composed env entries (`Key | Type | Value`, plus a `Source` column naming\nthe rule glob + companion when any overlay entry is present).\n\nJSON format:\n`{ \"base\": \"...\", \"companions\": [\"...\", ...], \"entries\": [{ \"key\", \"value\", \"type\"[,\n\"separator\"][, \"source\": { \"kind\": \"patch\", \"rule\", \"companion\" }] }, ...] }`."
    },
    "PatchTestEntry": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "value": {
          "type": "string"
        },
        "type": {
          "$ref": "#/$defs/ModifierKind"
        },
        "separator": {
          "type": "string",
          "description": "The separator a [`ModifierKind::List`] entry folds with; `None` on\nevery other kind. Skipped in JSON when `None`."
        },
        "source": {
          "description": "Provenance for a companion overlay entry; `None` for base-native entries.",
          "$ref": "#/$defs/EntrySource"
        }
      },
      "required": [
        "key",
        "value",
        "type"
      ],
      "description": "A single composed environment variable entry shown by `ocx patch test`.\n\nJSON format: `{ \"key\": \"...\", \"value\": \"...\", \"type\": \"constant\"|\"path\"|\"list\"[,\n\"separator\": \"...\"][, \"source\": { \"kind\": \"patch\", \"rule\": \"...\", \"companion\": \"...\" }] }`.\nThe optional `separator` field is present only for a `\"type\":\"list\"` entry. The\noptional `source` object is present only for companion overlay entries and\nnames the rule glob + companion that produced the entry; base-native entries\nomit it. Shares [`EntrySource`] with `--show-patches`."
    },
    "PatchWhyReport": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/PatchWhyEntry"
      }
    },
    "PatchWhyEntry": {
      "type": "object",
      "properties": {
        "variable": {
          "type": "string"
        },
        "rule": {
          "type": "string"
        },
        "companion": {
          "type": "string"
        }
      },
      "required": [
        "variable",
        "rule",
        "companion"
      ],
      "description": "A single env var an infrastructure-patch companion contributes to a base.\n\n`variable` is the env var name, `rule` is the descriptor rule `match` glob\nthat admitted the companion for the base, and `companion` is the companion\nidentifier whose interface projection produced the var."
    },
    "LocatedPaths": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/LocatedPath"
      }
    },
    "LocatedPath": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string"
        },
        "kind": {
          "$ref": "#/$defs/PathKind"
        }
      },
      "required": [
        "path",
        "kind"
      ],
      "description": "One located package: the directory `ocx package which` reports for it, and\nwhich kind of directory that is (contract C-016).\n\nA tool composed lazily has no package directory until its first invocation\nmaterializes one, so the answer to \"where is this on disk\" is its generated\nshim tree. [`PathKind`] is the discriminator that lets a consumer tell the\ntwo apart — minted once for the whole feature and shared with `ocx pull`'s\nreport, never re-spelled here."
    },
    "PathKind": {
      "oneOf": [
        {
          "type": "string",
          "const": "package",
          "description": "The package root: the parent of `content/` and `entrypoints/`."
        },
        {
          "type": "string",
          "const": "shim",
          "description": "The generated shim directory of a deferred tool. Its `bin/` holds one\nlauncher per declared name; the package directory does not exist yet."
        }
      ],
      "description": "What kind of directory a reported `path` names.\n\nA tool composed lazily has no package directory until its first invocation\nmaterializes one — what exists on disk is its generated shim tree. Reporting\nthe package directory for it would be a lie in a machine-read field, and\nreporting the shim directory without saying so would be a silent change of\nmeaning. The discriminator is what lets a consumer tell the two apart.\n\n**One definition, two consumers.** `ocx pull` (this crate's `command/pull.rs`)\nand `ocx package which` both answer \"where is this tool on disk\", and both\nanswer it with the same two-value vocabulary — so it lives here rather than\nbeing re-spelled in either report type."
    },
    "Paths": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "PullDryRun": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/DryRunEntry"
      }
    },
    "DryRunEntry": {
      "type": "object",
      "properties": {
        "package": {
          "$ref": "#/$defs/PinnedIdentifier"
        },
        "status": {
          "$ref": "#/$defs/PullStatus"
        },
        "path": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "package",
        "status",
        "path"
      ],
      "description": "A single dry-run preview row.\n\n`package` is held typed rather than pre-formatted so plain and JSON can\nrender it differently: `PinnedIdentifier`'s `Serialize` is its `Display`,\nso JSON keeps the pinned `…@sha256:<64hex>` form, while the plain table\ndrops the digest.\n\n`path` is `Some` for cached entries (the package root directory,\nparent of `content/` and `entrypoints/`) and `None` for `WouldFetch`\nrows where nothing has been materialised yet. Mirrors the contract\ndocumented on [`crate::api::data::paths::PathEntry`]: consumers\ntraverse into `<path>/content/` for installed files or\n`<path>/entrypoints/` for generated launchers."
    },
    "PullStatus": {
      "type": "string",
      "enum": [
        "cached",
        "would-fetch"
      ],
      "description": "Whether a locked tool is already in the object store or would be\nfetched on a real `ocx pull`."
    },
    "PushReport": {
      "type": "object",
      "properties": {
        "identifier": {
          "type": "string",
          "description": "The pushed package identifier (`registry/repository:tag`)."
        },
        "status": {
          "type": "string",
          "description": "Outcome of the push. Always `\"pushed\"`: the command performs the push\nunconditionally (the registry merge is idempotent)."
        },
        "manifest_digest": {
          "type": "string",
          "description": "Digest of the pushed multi-platform image index (`sha256:...`)."
        },
        "cascade_tags_written": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Rolling cascade tags written in addition to the primary version tag\n(e.g. `3.28`, `3`, `latest`). Empty for a non-cascade push."
        },
        "keep_tags_written": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Digest-named `__ocx.keep.<algorithm>-<hex>` tags this push wrote, in\npush order, one\nper distinct platform manifest — platforms whose manifest is identical\nshare a single tag, so this does not zip against the pushed platform\nlist. Empty under `--no-keep-tag`. Reports what reached the\nregistry, not what was requested."
        },
        "layers": {
          "$ref": "#/$defs/LayerCounts",
          "description": "Counts of layer-push outcomes (mounted/uploaded/verified), summed over\nevery platform this push fanned out to. Layer blobs only — the config\nblob and manifest are not layers."
        },
        "platform_digests": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Platform manifest digests this push produced, keyed by the canonical\nplatform string (`os/arch[/variant][+feature,…]`). The signing input\nfor a later `push --sign`, and independent of `--keep-tag`: a\n`--no-keep-tag` push reports an empty `keep_tags_written` and this map\nfully populated.\n\nDistinct from `manifest_digest`, which names the tag's image index —\nrewritten on every platform merge, and therefore not what a signature\ncan cover.\n\nA `BTreeMap` rather than an array: the access a consumer wants is\n`.platform_digests[\"linux/amd64\"]`, and sorted output is\ndeterministic. Two platforms sharing one manifest appear as two keys\nwith one value. JSON only — the plain table is already at its\nfive-column budget and a digest column would blow the width."
        },
        "signatures": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SignedPlatformReport"
          },
          "description": "One row per platform manifest `--sign` signed inline, in push order.\n\nEmpty (and omitted) without `--sign`, so the key set a consumer of an\nunsigned push parses is unchanged. JSON only, exactly like\n`platform_digests` and `attestation`: the plain table is at its\nfive-column budget, and a signing failure additionally reaches a plain\ncaller on stderr.\n\nThe index is deliberately absent from this list. `push` signs the\nplatform manifests because their digests are final the moment they are\npushed; the index digest is rewritten on every platform merge, so it is\nsigned later by `ocx package sign --tags-file`."
        },
        "attestation": {
          "description": "`None` unless `--sbom` was passed.\n\nAdditive: `ocx-mirror pipeline push` keys its go/no-go off `status`, and\n`status` still reports the push alone. A push that lands and an\nattestation that then fails is a real state — the manifest is immutable\nand OCI offers no un-push — so the two outcomes are reported separately\nrather than folded into one verdict.",
          "$ref": "#/$defs/AttestationOutcome"
        }
      },
      "required": [
        "identifier",
        "status",
        "manifest_digest",
        "cascade_tags_written",
        "keep_tags_written",
        "layers"
      ],
      "description": "Result of a successful `ocx package push`.\n\nPlain format: a one-row table — `Identifier`, `Digest`, `Tags` (the rolling\ncascade tags, comma-joined), `Keep Tags` (how many were written) and\n`Layers` (the mounted/uploaded/verified counts). Keeps a plain push from\nbeing silent; progress still surfaces on stderr via the log layer. `status`\nis plain-omitted (it is the constant `\"pushed\"`) and the keep tags are\ncounted rather than listed — each is an 82-column\n`__ocx.keep.sha256-<hex>` and there is one per distinct platform manifest.\n\nJSON format:\n`{ \"identifier\", \"status\", \"manifest_digest\", \"cascade_tags_written\",\n\"keep_tags_written\", \"layers\": { \"mounted\", \"uploaded\", \"verified\" },\n\"platform_digests\": { \"<platform>\": \"sha256:…\" } }`.\nThe first five keys are the machine-readable contract consumed by\n`ocx-mirror pipeline push`, which keys its go/no-go bookkeeping off `status`\nand records `cascade_tags_written` in the run summary; `layers` and\n`platform_digests` are additive."
    },
    "LayerCounts": {
      "type": "object",
      "properties": {
        "mounted": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Layers a cross-repository blob mount placed in the target repository\nwithout any upload."
        },
        "uploaded": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Layers sent through `push_blob` — see [`LayerPushOutcome::Uploaded`]\nfor why this does not strictly mean \"bytes crossed the wire\"."
        },
        "verified": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "`LayerRef::Digest` layers confirmed present by `head_blob`."
        }
      },
      "required": [
        "mounted",
        "uploaded",
        "verified"
      ],
      "description": "Aggregate counts of layer-push outcomes for a single package push.\n\nOnly layer blobs are counted — the config blob and the manifest itself\nare not layers and are excluded. An `uploaded` count may still have\nHEAD-skipped an already-present blob inside `push_blob` (see\n[`LayerPushOutcome::Uploaded`]); this struct distinguishes mount vs.\nexplicit-upload vs. verify-by-digest at the `push_multi_layer_manifest`\ncall site, not whether bytes actually crossed the wire.\n\n`Serialize` derives directly on this type (rather than a CLI-side\nwrapper) so `ocx_cli`'s `PushReport` can embed it verbatim as the\n`layers` field of the push JSON report."
    },
    "SignedPlatformReport": {
      "type": "object",
      "properties": {
        "platform": {
          "type": "string",
          "description": "The platform whose manifest was signed, canonically spelled\n(`os/arch[/variant][+feature,…]`) — the same key `platform_digests`\nuses, so the two zip."
        },
        "status": {
          "$ref": "#/$defs/SweptStatus",
          "description": "What the inline signing did to this platform."
        },
        "report": {
          "description": "That platform's own sign report, verbatim. Present for every platform\nwhose run produced one, `failed` rows included: a\n`--signature-format both` platform where one leg landed and one did not\nis a failure that still carries the leg that landed.",
          "$ref": "#/$defs/SignatureReport"
        },
        "kind": {
          "type": "string",
          "description": "The JSON error envelope's per-variant slug for this platform's failure,\nfalling back to its frozen category. Present exactly when `status` is\n`failed`."
        },
        "message": {
          "type": "string",
          "description": "Human-readable cause, sanitized for the terminal (CWE-150). Present\nexactly when `status` is `failed`."
        }
      },
      "required": [
        "platform",
        "status"
      ],
      "description": "One platform manifest's inline-signing row.\n\nThe [`SweptTagReport`] shape with `tag` replaced by `platform`, and for the\nsame reasons: flat rather than an internally-tagged enum, because `report`\nis a struct that a tagged enum could only carry through `flatten`, and a\n`status` plus three optionals is the same information without that. It\nreuses [`SweptStatus`] so a reader meets one vocabulary across `sign`'s\nsweep and `push`'s inline signing, and [`SignatureReport`] verbatim so a\nconsumer parsing a `ocx package sign` document parses this one with the\nsame code, one level down.\n\n`platform` rather than `tag` is the one honest difference: `push` signs the\nplatform manifests a push landed on, and putting `linux/amd64` in a field\nevery other report spells `tag` would name an OCI tag that does not exist.\n`SweptStatus::Skipped` is unreachable here — a platform the merged index did\nnot carry is omitted from `platform_digests`, so it never becomes a row.\n\n[`SweptTagReport`]: crate::api::data::sweep::SweptTagReport"
    },
    "SweptStatus": {
      "oneOf": [
        {
          "type": "string",
          "const": "completed",
          "description": "The tag's index was acted on; `report` carries the outcome."
        },
        {
          "type": "string",
          "const": "skipped",
          "description": "The tag resolved to a bare manifest, so the sweep left it alone.\n\nNot a failure, and it does not make the run exit non-zero: `push`\nalready signed each platform manifest inline, and a tag list mixing\nsingle-platform and multi-platform packages is the normal case for a\nrepository publishing both."
        },
        {
          "type": "string",
          "const": "failed",
          "description": "This tag failed. The sweep carried on to the rest and the run exits\nnon-zero at the end."
        }
      ],
      "description": "What the sweep did to one tag.\n\nOne vocabulary for both verbs. `completed` rather than `signed` on purpose:\n`ocx package attest` can attach an unsigned statement when the run has no\nsigning material at all, and a status that claimed `signed` there would\ncontradict the very report it sits next to."
    },
    "SignatureReport": {
      "type": "object",
      "properties": {
        "identifier": {
          "type": "string",
          "description": "User-facing identifier string that was signed (echoes the CLI arg)."
        },
        "subject_digest": {
          "$ref": "#/$defs/Digest",
          "description": "Digest of the subject manifest that the bundle signs."
        },
        "legs": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SignatureLegReport"
          },
          "description": "One entry per wire shape that was written or attempted, in write order.\n\n`--signature-format both` emits two **independent** signatures, so the\nrun is best-effort per leg rather than atomic (spec D8): a leg that\nfailed is reported alongside one that succeeded, and the exit code comes\nfrom the failure. Hiding the successful leg behind the failure would\nleave the operator re-signing what is already published."
        },
        "platform": {
          "type": "string",
          "description": "Platform narrowed into (e.g., `linux/amd64`), or `any` when\n`--platform` was absent and the run signed whatever resolved."
        },
        "signer": {
          "type": "string",
          "description": "Signing mechanism used (C-S1-1 contract field). Always `\"keyless-fulcio\"` in Slice 1."
        },
        "certificate_identity": {
          "type": "string",
          "description": "Certificate SAN (identity) embedded in the Fulcio cert."
        },
        "certificate_oidc_issuer": {
          "type": "string",
          "description": "Certificate OIDC issuer URL embedded in the Fulcio cert."
        },
        "key_backend": {
          "$ref": "#/$defs/KeyBackendKind",
          "description": "Which key model produced this signature (`keyless`, `file`, and — once\nthey exist — `aws_kms` and friends).\n\nA consumer can already distinguish `file` from a future `awskms` without\nthe backends existing, which is the point of freezing the vocabulary\nbefore the implementations (spec §WP9 contract 4)."
        },
        "public_key_hint": {
          "type": "string",
          "description": "The signing key's cosign hint, in key mode only."
        },
        "transparency_log_index": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0,
          "description": "Whether a transparency record was created, and its log index when so.\n\n**Emitted unconditionally**, `null` included: under a key\n`--rekor-upload` is opt-in, so the absence of a record is a legal\noutcome the operator must be able to *see* rather than infer from a\nmissing key."
        }
      },
      "required": [
        "identifier",
        "subject_digest",
        "legs",
        "platform",
        "signer",
        "certificate_identity",
        "certificate_oidc_issuer",
        "key_backend",
        "transparency_log_index"
      ],
      "description": "Summary of a signing operation, keyless or under a key.\n\nPlain format: single \"Field | Value\" table listing the identifier, subject\ndigest, one `Signature (<format>)` row per leg, platform, key model, cert\nidentity, and cert OIDC issuer (one row per field — `Printable`\nsingle-table rule honored). `subject_digest` is the answer (what was\nsigned) and renders full; a leg's digests shorten to 12 hex — a full\n`sha256:<64hex>` earns its row only once per view. Everything stays full\nin JSON.\n\nA leg's two digests are distinct and not interchangeable: `payload_digest`\nis the SHA-256 of the signed blob (the Sigstore bundle under `bundle`, the\nsimplesigning claim under `simplesigning` — what the transparency record\ncovers), while `manifest_digest` is the SHA-256 of the manifest it hangs\nfrom (the OCI referrer, or the `sha256-<hex>.sig` sidecar). Consumers\nroutinely need one or the other.\n\n`signer` is the signing mechanism used: `\"keyless-fulcio\"`, or the key\nbackend's own slug under a key."
    },
    "SignatureLegReport": {
      "type": "object",
      "properties": {
        "format": {
          "$ref": "#/$defs/SignatureFormat",
          "description": "The shape: `bundle` or `simplesigning`."
        },
        "payload_digest": {
          "description": "Digest of the signed payload blob — the Sigstore bundle under `bundle`,\nthe simplesigning claim under `simplesigning`. Absent when the leg failed.",
          "$ref": "#/$defs/Digest"
        },
        "manifest_digest": {
          "description": "Digest of the manifest the payload hangs from — the OCI referrer under\n`bundle`, the `sha256-<hex>.sig` sidecar under `simplesigning`.",
          "$ref": "#/$defs/Digest"
        },
        "error": {
          "type": "string",
          "description": "Why the leg failed, when it did. `None` means it was written."
        }
      },
      "required": [
        "format"
      ],
      "description": "One wire shape's outcome, as reported."
    },
    "SignatureFormat": {
      "oneOf": [
        {
          "type": "string",
          "const": "bundle",
          "description": "An OCI 1.1 referrer carrying a Sigstore bundle v0.3. The default, and\nthe only shape `cosign sign` v3 produces against a registry that\nimplements the Referrers API."
        },
        {
          "type": "string",
          "const": "simplesigning",
          "description": "The cosign `sha256-<hex>.sig` sidecar tag, whose layers carry\nsimplesigning payloads."
        },
        {
          "type": "string",
          "const": "both",
          "description": "Write both shapes.\n\n**Write-side only.** A verify pins exactly one shape, because \"either\nof two signatures satisfied me\" is not a statement a verification\nresult can carry; the read-side resolver refuses this value."
        }
      ],
      "description": "Which cosign wire shape a signature is written in, and which shape a verify\npins.\n\nFormat and key model are **orthogonal**: either format can be produced\nkeyless or with a key pair, so this enum never says anything about how the\nsigning material was obtained.\n\nOne vocabulary, two channels — serde for config and JSON, `ValueEnum` for\nthe command line. The two spellings are identical by contract, and\n`signature_format_slugs_are_frozen` asserts that rather than assuming it:\nthe enum is hand-written on the clap side (`ocx_lib` carries\n`clap_builder`, not the `clap` derive), so nothing makes the two agree\nautomatically."
    },
    "AttestationOutcome": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "referrer_digest": {
              "type": "string",
              "description": "Digest of the published OCI referrer manifest. Absent under\n`--signature-format simplesigning`, which publishes the\n`sha256-<hex>.att` sidecar alone and no referrer."
            },
            "sidecar_digest": {
              "type": "string",
              "description": "Digest of the `sha256-<hex>.att` sidecar manifest, when\n`--signature-format` asked for one. The spelling is\n[`AttestationReport`](crate::api::data::attestation::AttestationReport)'s,\nso one vocabulary describes the same two addresses in both reports."
            },
            "predicate_type": {
              "type": "string",
              "description": "The resolved `predicateType` URI written into the Statement."
            },
            "signed": {
              "type": "boolean",
              "description": "Whether the referrer carries a signature. `false` means the SBOM\nwas attached raw because the run had no signing identity available\n— the push still succeeded, and nothing vouches for the document."
            },
            "status": {
              "type": "string",
              "const": "succeeded"
            }
          },
          "required": [
            "predicate_type",
            "signed",
            "status"
          ],
          "description": "The attestation was published on the pushed manifest."
        },
        {
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "description": "The error envelope's per-variant slug (`error.detail`), falling\nback to its frozen category (`error.kind`) for errors outside the\nsign and verify taxonomies, which carry no `detail`."
            },
            "message": {
              "type": "string",
              "description": "Human-readable cause, sanitized for the terminal."
            },
            "status": {
              "type": "string",
              "const": "failed"
            }
          },
          "required": [
            "kind",
            "message",
            "status"
          ],
          "description": "The push landed and was NOT rolled back; the attestation did not."
        }
      ],
      "description": "What `--sbom` did after the push landed.\n\nA failure carries the error slug the JSON error envelope would have used\n(CLI-04), not a bespoke string, so a script branches on the same vocabulary\neither way."
    },
    "Removed": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/RemovedEntry"
      }
    },
    "RemovedEntry": {
      "type": "object",
      "properties": {
        "package": {
          "type": "string"
        },
        "status": {
          "$ref": "#/$defs/RemovedStatus"
        },
        "path": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "package",
        "status",
        "path"
      ],
      "description": "A single uninstall or deselect result entry.\n\nThe `path` field holds the symlink that was removed (for `Removed`),\nthe object directory that was purged (for `Purged`), or is `None`\nwhen the resource was already absent."
    },
    "RemovedStatus": {
      "type": "string",
      "enum": [
        "removed",
        "purged",
        "absent"
      ],
      "description": "Whether the resource was actually removed, purged, or was already absent."
    },
    "SbomListingReport": {
      "type": "object",
      "properties": {
        "summary": {
          "$ref": "#/$defs/ListingSummary",
          "description": "One-glance counts, so a consumer branches on a field instead of\nmeasuring an array (PKG-25)."
        },
        "entries": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SbomEntry"
          },
          "description": "One entry per verified attestation, in listing order."
        },
        "refused": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/RefusedEntry"
          },
          "description": "Every candidate examined and refused, in listing order. Never\ntruncated in JSON."
        }
      },
      "required": [
        "summary",
        "entries",
        "refused"
      ],
      "description": "Every verified attestation a package carries, plus what was refused."
    },
    "ListingSummary": {
      "type": "object",
      "properties": {
        "status": {
          "type": "string",
          "description": "`success` when nothing was refused, `partial_failure` otherwise."
        },
        "verification": {
          "$ref": "#/$defs/ListingVerification",
          "description": "Which trust contract produced this listing. See\n[`ListingVerification`]."
        },
        "exit_code": {
          "type": "integer",
          "format": "uint8",
          "minimum": 0,
          "maximum": 255,
          "description": "Mirrors the process exit code. Always 0 here, as a posture rather than\nas an unreachability claim: a refusal beside a listing is a partial\nfailure the caller is told about and still exits 0 for, and under\n`--summary` that includes a listing whose `entries` ended up empty\nbecause every document refused. Only the library's own zero-match scan\nexits non-zero — `AttestationNotFound` (79) — and it never reaches a\nreport at all."
        },
        "total": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "`verified + unverified + refused` — every candidate the scan examined."
        },
        "verified": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Attestations that passed every check."
        },
        "unverified": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Documents no signature was checked for. Counted apart from\n[`Self::verified`] so a script branches on the trust class instead of\nfiltering the array — an unverified document is a real answer to \"what\nSBOMs does this carry\" and not a real answer to \"who vouches for them\"."
        },
        "refused": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Candidates examined and refused."
        }
      },
      "required": [
        "status",
        "verification",
        "exit_code",
        "total",
        "verified",
        "unverified",
        "refused"
      ],
      "description": "The counts and the status a script branches on."
    },
    "ListingVerification": {
      "oneOf": [
        {
          "type": "string",
          "const": "verified",
          "description": "Signatures were checked against the resolved policies."
        },
        {
          "type": "string",
          "const": "unverified",
          "description": "Nothing was checked; every row is unverified."
        }
      ],
      "description": "Which trust contract the whole listing was produced under.\n\nA script needs this to read the rows correctly: `unverified` rows mean two\ndifferent things depending on it. Under [`Self::Verified`] an unverified row\ncannot occur at all (an unsigned attachment is refused, not listed), so\nevery entry carries a checked signature. Under [`Self::Unverified`] nothing\nwas checked and every entry is unverified regardless of whether a publisher\nsigned it — a signed SBOM read this way is reported exactly like an\nunsigned one, because this run has no evidence to tell them apart.\n\nDeliberately the same vocabulary as the per-entry `verified` flag rather\nthan the internal mode names, so one word means one thing at both levels."
    },
    "SbomEntry": {
      "type": "object",
      "properties": {
        "predicate_type": {
          "type": "string",
          "description": "predicateType. Read out of the **signed** payload when\n[`Self::verified`]; derived from the referrer's `artifactType`\notherwise, since an unsigned referrer states its type nowhere else."
        },
        "verified": {
          "type": "boolean",
          "description": "Whether a signature was verified over this document.\n\n`false` means the SBOM was attached raw, with no identity behind it:\nthe registry served bytes and said what they are. The three fields\nbelow are then absent rather than empty."
        },
        "shadowed": {
          "type": "boolean",
          "description": "`true` when a platform-level SBOM of the **same predicateType**\nsupersedes this index-level one. A shadowed entry stays listed under\n`--format json`; only the human default collapses to the preferred one.\n\nEmitted unconditionally, unlike the optional fields below and unlike\n`VerificationReport::signatures`: `false` is a *true* statement here —\nnothing supersedes this document — where an omitted-while-empty array\nwould be a claim that we had looked. A consumer can therefore branch on\nthe key without first testing for its presence."
        },
        "subject_digest": {
          "type": "string",
          "description": "The target digest. Proven bound by the signed Statement when\n[`Self::verified`]; claimed by the referrer otherwise."
        },
        "referrer_digest": {
          "type": "string",
          "description": "What carried the document — and **not always a manifest**. Almost\nalways the OCI referrer manifest's digest; the **layer** blob's digest\nin exactly one case, a verified attestation read off a cosign\n`sha256-<hex>.att` sidecar tag, where one layer is one document and the\nmanifest digest would name all of them at once. A consumer that\naddresses it as `GET /v2/<name>/manifests/<digest>` therefore 404s on\nthat case.\n\nNarrower than the verify side's rule, and for a structural reason: an\nSBOM scan runs under `VerifyContentMode::Attestation`, which leaves\n`discover_simplesigning` false, so the `.sig` sidecar door — the one\nthat reports a layer digest while claiming `referrers_api`, because it\ninherits the *listing's* discovery method — is never opened here. The\nonly layer digest reachable is the `.att` reader's, and that door is\ntag-addressed by construction.\n\nThis row carries neither `signature_format` nor `discovery_method`, so\nthere is nothing here to branch on: a consumer that must address the\ndigest reads the same subject through\n`ocx package verify --attestation --format json`, whose `signatures[]`\nrows carry the discriminator."
        },
        "certificate_identity": {
          "type": "string",
          "description": "Certificate SAN (identity) embedded in the Fulcio cert."
        },
        "certificate_oidc_issuer": {
          "type": "string",
          "description": "Certificate OIDC issuer embedded in the Fulcio cert."
        },
        "signed_at": {
          "type": "string",
          "description": "Rekor integrated time, RFC 3339 with an explicit `Z` (PLAT-31)."
        },
        "summary": {
          "description": "Populated only under `--summary`.",
          "$ref": "#/$defs/SbomSummaryOut"
        }
      },
      "required": [
        "predicate_type",
        "verified",
        "shadowed",
        "subject_digest",
        "referrer_digest"
      ],
      "description": "One verified attestation."
    },
    "SbomSummaryOut": {
      "type": "object",
      "properties": {
        "spec_version": {
          "type": "string",
          "description": "The document's own `specVersion`, verbatim."
        },
        "serial_number": {
          "type": "string",
          "description": "`serialNumber`, when the document carries one."
        },
        "component_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Length of the top-level `components` array."
        },
        "top_level_component": {
          "type": "string",
          "description": "`metadata.component.name`."
        }
      },
      "required": [
        "spec_version",
        "component_count"
      ],
      "description": "What `--summary` reports for one CycloneDX document."
    },
    "RefusedEntry": {
      "type": "object",
      "properties": {
        "referrer_digest": {
          "type": "string",
          "description": "The referrer's digest, verbatim as the registry listed it."
        },
        "reason": {
          "type": "string",
          "description": "Why this candidate was refused, as prose for a human. Registry-sourced\neither way: several kinds quote a field read off the wire."
        },
        "reason_kind": {
          "type": "string",
          "description": "The same refusal as a frozen slug (`VerifyErrorKind::kind_detail`).\n\nPKG-25: a script branches on this, never on [`Self::reason`] — the\nprose is English, it is free to be reworded, and substring-matching it\nis how a consumer silently stops matching. `&'static str` on purpose:\nthe only values that fit are the ones the frozen table produces."
        }
      },
      "required": [
        "referrer_digest",
        "reason",
        "reason_kind"
      ],
      "description": "One candidate that was examined and refused."
    },
    "ScriptRunReport": {
      "type": "object",
      "properties": {
        "status": {
          "$ref": "#/$defs/ScriptStatus",
          "description": "Overall status (the structured mirror of the exit code)."
        },
        "assertion": {
          "anyOf": [
            {
              "$ref": "#/$defs/AssertionRecord"
            },
            {
              "type": "null"
            }
          ],
          "description": "The terminating assertion record, if the run failed on an assertion."
        },
        "run": {
          "anyOf": [
            {
              "$ref": "#/$defs/RunSummary"
            },
            {
              "type": "null"
            }
          ],
          "description": "Surfaced `RunResult` fields, when a terminal `ocx.run` is reported."
        }
      },
      "required": [
        "status",
        "assertion",
        "run"
      ],
      "description": "The `--script` result envelope.\n\nPlain format: one human status line on stdout.\n\nJSON format: `{\"status\", \"assertion\", \"run\"}` — `assertion`/`run` are\n`null` when not applicable."
    },
    "ScriptStatus": {
      "oneOf": [
        {
          "type": "string",
          "const": "passed",
          "description": "Script ran to completion; all assertions passed."
        },
        {
          "type": "string",
          "const": "failed",
          "description": "An assertion failed, `expect.fail`/`fail()`, or a host-fn failure."
        },
        {
          "type": "string",
          "const": "usage",
          "description": "The script could not be used as given (unreadable script file)."
        },
        {
          "type": "string",
          "const": "script_error",
          "description": "Syntax / arity / type error in the script source."
        },
        {
          "type": "string",
          "const": "io",
          "description": "A sandboxed filesystem operation failed for I/O reasons."
        },
        {
          "type": "string",
          "const": "timeout",
          "description": "The wall-clock budget elapsed before the script finished."
        }
      ],
      "description": "Overall outcome of a scripted test run. Mirrors\n`ocx_lib::script::ScriptOutcomeKind` at the OCX-facing level."
    },
    "AssertionRecord": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Assertion kind (e.g. `ok`, `eq`, `contains`)."
        },
        "message": {
          "type": "string",
          "description": "Failure detail. For `expect.ok`, auto-embeds the child stderr."
        }
      },
      "required": [
        "kind",
        "message"
      ],
      "description": "The terminating assertion record (when the run failed on an assertion).\n\n`message` prose is non-stable; its presence and field shape are contractual."
    },
    "RunSummary": {
      "type": "object",
      "properties": {
        "exit_code": {
          "type": "integer",
          "format": "int32",
          "description": "Child exit code (or `128 + signal` when signal-killed)."
        },
        "stdout": {
          "type": "string",
          "description": "Captured stdout (possibly truncated)."
        },
        "stderr": {
          "type": "string",
          "description": "Captured stderr (possibly truncated)."
        },
        "duration_ms": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "description": "Wall-clock duration in milliseconds."
        },
        "truncated": {
          "type": "boolean",
          "description": "`true` iff stdout or stderr hit the capture cap."
        }
      },
      "required": [
        "exit_code",
        "stdout",
        "stderr",
        "duration_ms",
        "truncated"
      ],
      "description": "Surfaced `RunResult` fields for the script's terminal/top-level `ocx.run`."
    },
    "SelfSetupData": {
      "type": "object",
      "properties": {
        "status": {
          "$ref": "#/$defs/StatusKind"
        },
        "bootstrap": {
          "$ref": "#/$defs/BootstrapEntry"
        },
        "shims": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "profiles": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ProfileEntry"
          }
        },
        "dirty_profiles": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Profiles skipped because the user edited the managed block; present iff\nstatus = `skipped`. Carried separately for a script to `case` on without\nscanning the `profiles` list."
        },
        "exec_policy_warning": {
          "type": "string",
          "description": "Windows execution-policy `Restricted` advisory, if applicable."
        },
        "conflicting_ocx": {
          "type": "string",
          "description": "An `ocx` on `PATH` ahead of the directory the shim prepends, if found."
        },
        "reload_hint": {
          "type": "boolean",
          "description": "Whether the user should re-source their profile to activate ocx now."
        },
        "managed_config": {
          "$ref": "#/$defs/ManagedConfigEntry",
          "description": "Result of adopting/clearing the `--managed-config` tier (phase 1.5)."
        }
      },
      "required": [
        "status",
        "bootstrap",
        "shims",
        "profiles",
        "managed_config"
      ],
      "description": "CLI wrapper around [`SetupOutcome`] for API reporting.\n\nPlain format: a key/value table — `Status`, `Bootstrap`, written shims,\nper-profile outcomes, and any advisory (exec-policy / conflicting ocx /\nreload hint) — with empty rows suppressed.\n\nJSON format (discriminated by `status`):\n- `{\"status\":\"completed\",\"bootstrap\":{…},\"shims\":[…],\"profiles\":[{\"path\":\"…\",\"outcome\":\"completed\"}],\"managed_config\":{\"status\":\"not_configured\"}}`\n- dirty → `{\"status\":\"skipped\",…,\"dirty_profiles\":[\"…\"]}`\n- `managed_config` is always present: `{\"status\":\"…\"}`, plus `\"digest\"` on\n  the adopt/refresh paths (`adopted` / `already_adopted` / `refreshed` /\n  `refresh_unavailable` / `would_refresh`), `\"previous_digest\"` on\n  `refreshed`, and `\"reason\"` on `refresh_unavailable`.\n- `exec_policy_warning`, `conflicting_ocx`, and `reload_hint` appear only\n  when present."
    },
    "StatusKind": {
      "oneOf": [
        {
          "type": "string",
          "const": "completed",
          "description": "Shims and/or profiles were written or upgraded."
        },
        {
          "type": "string",
          "const": "no_op",
          "description": "Nothing changed — every shim and profile was already current."
        },
        {
          "type": "string",
          "const": "skipped",
          "description": "At least one profile carried user edits and was left untouched."
        },
        {
          "type": "string",
          "const": "migrated",
          "description": "A legacy footprint was migrated to the v1 fence."
        }
      ],
      "description": "Top-level status discriminant for a `ocx self setup` run.\n\nSerde serializes each variant to its `snake_case` name, matching the JSON\nwire format the snapshot tests pin. `Skipped` is the dirty-RC outcome (the\nCLI maps it to exit 82); every other status is exit 0."
    },
    "BootstrapEntry": {
      "type": "object",
      "properties": {
        "status": {
          "$ref": "#/$defs/ApiBootstrapStatus"
        },
        "version": {
          "type": "string"
        },
        "digest": {
          "type": "string",
          "description": "Resolved content digest; present when pinning produced one.\n\nStringified at this API boundary — lib carries [`ocx_lib::oci::Digest`]."
        }
      },
      "required": [
        "status"
      ],
      "description": "JSON-serialized bootstrap outcome.\n\nUnpinned path (no VERSION): `{\"status\":\"already_present\"}` or\n`{\"status\":\"pulled\",\"version\":\"1.2.3\"}`.\nPinned path (VERSION given): same shapes plus `\"digest\":\"sha256:<hex>\"` when\nresolution produced one. `digest` is omitted on unpinned fast-path runs so\nexisting JSON consumers stay byte-identical (plan D7)."
    },
    "ApiBootstrapStatus": {
      "type": "string",
      "enum": [
        "already_present",
        "pulled",
        "would_pull"
      ],
      "description": "API-layer status discriminant (mirrors `ocx_lib::setup::BootstrapStatus`).\n\nNamed `ApiBootstrapStatus` to avoid shadowing the lib type imported above."
    },
    "ProfileEntry": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string"
        },
        "outcome": {
          "$ref": "#/$defs/ProfileOutcomeKind"
        }
      },
      "required": [
        "path",
        "outcome"
      ],
      "description": "JSON-serialized per-profile outcome (`{\"path\":\"…\",\"outcome\":\"completed\"}`)."
    },
    "ProfileOutcomeKind": {
      "type": "string",
      "enum": [
        "completed",
        "no_op",
        "migrated",
        "skipped_dirty"
      ],
      "description": "Serde-facing mirror of [`ProfileOutcome`] (`snake_case` discriminant)."
    },
    "SelfUpdateData": {
      "type": "object",
      "properties": {
        "status": {
          "$ref": "#/$defs/StatusKind2"
        },
        "from": {
          "type": "string",
          "description": "Previously installed version; present iff status = `installed` AND the\nsubprocess version query succeeded. `None` indicates the subprocess\ninvocation was not available (binary absent, non-zero exit, malformed\nJSON output)."
        },
        "to": {
          "type": "string",
          "description": "Newly installed version; present iff status = `installed`."
        },
        "skipped_reason": {
          "description": "Why the update was skipped; present iff status = `skipped`. Carries the\nstructured [`SkippedReason`] enum so scripts can dispatch on `reason`\nwithout string parsing.",
          "$ref": "#/$defs/SkippedReason"
        }
      },
      "required": [
        "status"
      ],
      "description": "CLI wrapper around [`SelfUpdateResult`] for API reporting.\n\nJSON format (discriminated by `status`):\n- `{\"status\": \"up_to_date\"}` — no payload\n- `{\"status\": \"installed\", \"from\": \"0.0.1\", \"to\": \"0.0.2\"}` (`from` omitted\n  when subprocess version query failed — bootstrap mode)\n- `{\"status\": \"skipped\", \"skipped_reason\": {\"reason\": \"<variant>\"[, \"detail\": \"…\"]}}`\n\nPlain format: key/value table with conditional rows; `Status` always\npresent, `From`/`To`/`Skipped reason` appear only when applicable."
    },
    "StatusKind2": {
      "type": "string",
      "enum": [
        "up_to_date",
        "skipped",
        "update_available",
        "installed"
      ],
      "description": "Typed status discriminant shared by [`UpdateCheckData`] and [`SelfUpdateData`].\n\nPrevents typos at compile time (a stringly-typed `&'static str` would accept\n`\"up_to-date\"` silently). Serde serializes each variant to its `snake_case`\nname, matching the JSON wire format pinned by the snapshot tests."
    },
    "SkippedReason": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "reason": {
              "type": "string",
              "const": "bootstrap"
            }
          },
          "required": [
            "reason"
          ],
          "description": "The subprocess version query failed: binary absent (true bootstrap),\nnon-zero exit, or unparseable JSON output. The check cannot compare\nversions and is skipped; the install path proceeds anyway with\n`from = None`."
        },
        {
          "type": "object",
          "properties": {
            "reason": {
              "type": "string",
              "const": "offline"
            }
          },
          "required": [
            "reason"
          ],
          "description": "The operation was blocked by offline mode."
        },
        {
          "type": "object",
          "properties": {
            "reason": {
              "type": "string",
              "const": "throttled"
            }
          },
          "required": [
            "reason"
          ],
          "description": "The 24-hour throttle window has not elapsed since the last probe."
        },
        {
          "type": "object",
          "properties": {
            "reason": {
              "type": "string",
              "const": "registry_probe_failed"
            },
            "detail": {
              "type": "string"
            }
          },
          "required": [
            "reason",
            "detail"
          ],
          "description": "The registry probe returned an error; the error context is carried\nas the inner string."
        },
        {
          "type": "object",
          "properties": {
            "reason": {
              "type": "string",
              "const": "not_found"
            }
          },
          "required": [
            "reason"
          ],
          "description": "The package was not found in the remote registry."
        },
        {
          "type": "object",
          "properties": {
            "reason": {
              "type": "string",
              "const": "unparseable_current"
            },
            "detail": {
              "type": "string"
            }
          },
          "required": [
            "reason",
            "detail"
          ],
          "description": "The currently installed version string could not be parsed as a\nsemver-like version.  The unparseable string is carried as context."
        },
        {
          "type": "object",
          "properties": {
            "reason": {
              "type": "string",
              "const": "unparseable_latest"
            }
          },
          "required": [
            "reason"
          ],
          "description": "The latest tag returned by the registry could not be parsed as a\nsemver-like version."
        },
        {
          "type": "object",
          "properties": {
            "reason": {
              "type": "string",
              "const": "no_release_tag"
            }
          },
          "required": [
            "reason"
          ],
          "description": "No clean release tag (`major.minor.patch`) was found in the remote\ntag list."
        }
      ],
      "description": "The reason an update check was skipped.\n\nUsed as the payload of [`UpdateCheckResult::Skipped`] and\n[`SelfUpdateResult::Skipped`] so programmatic consumers (JSON, scripts)\ncan distinguish skip causes without string parsing.\n\nJSON serialization produces a discriminated object:\n- Unit variants: `{\"reason\": \"bootstrap\"}`\n- Variants with detail: `{\"reason\": \"registry_probe_failed\", \"detail\": \"…\"}`"
    },
    "UpdateCheckData": {
      "type": "object",
      "properties": {
        "status": {
          "$ref": "#/$defs/StatusKind2"
        },
        "identifier": {
          "type": "string",
          "description": "Identifier of the available update; present iff status = `update_available`."
        },
        "skipped_reason": {
          "description": "Why the check was skipped; present iff status = `skipped`. Carries the\nstructured [`SkippedReason`] enum so scripts can dispatch on `reason`\nwithout string parsing.",
          "$ref": "#/$defs/SkippedReason"
        }
      },
      "required": [
        "status"
      ],
      "description": "CLI wrapper around [`UpdateCheckResult`] for API reporting.\n\nJSON format (discriminated by `status`):\n- `{\"status\": \"up_to_date\"}` — no payload\n- `{\"status\": \"update_available\", \"identifier\": \"<id>\"}` — newer version\n  available at `identifier`\n- `{\"status\": \"skipped\", \"skipped_reason\": {\"reason\": \"<variant>\"[, \"detail\": \"…\"]}}`\n  — structured `SkippedReason` discriminator, programmatic without string parsing\n\nPlain format: key/value table; rows with no payload (e.g. `identifier` when\nstatus = `up_to_date`) are suppressed."
    },
    "ShellStateReport": {
      "type": "object",
      "properties": {
        "ocx_home": {
          "type": "string",
          "description": "`$OCX_HOME`, and whether it exists. A missing home is an ordinary state\non a fresh install and exits 0; only a home that cannot be *read* is the\nsingle 74 path (C-051)."
        },
        "ocx_home_present": {
          "type": "boolean",
          "description": "Whether `$OCX_HOME` exists on disk."
        },
        "shell_integration_installed": {
          "type": "boolean",
          "description": "Whether `ocx self setup` has ever wired this machine's shell — probed\nas [`ocx_lib::setup::shims::WITNESS_SHIM`] under [`Self::ocx_home`].\n\nC-050 reason 6's first-prompt half tells the user \"the next prompt\napplies it\". That is true only once the rc/profile fence exists to\nsource the shim, and on a bare-binary install neither does. Without\nthis field the report answers the command's single most likely first\nuse — *\"I installed ocx, why isn't it working\"* — with a fix that can\nnever come true, so the renderer branches on it rather than promising\nconvergence that nothing will perform.\n\nDeliberately **not** a [`Reason`] arm: \"setup has not run\" is not a\nconsent state, and `consent::Reason` enumerates consent states."
        },
        "lock_refusal": {
          "type": [
            "string",
            "null"
          ],
          "description": "Why the project's `ocx.lock` refuses composition, when it does — the\n`Display` of [`ocx_lib::project::LockCurrency`], rendered as text the\nsame way [`Note::ProjectUnresolved`] carries its detail.\n\nConsent can say *activate* over a lock that composition then refuses: a\n`paths` grant holds without a readable lock at all, and a stale one\nstill parses. Every prompt then exits 65 with a stderr the hook\ndiscards (A-21), so the scope never reaches the ledger and the report\nanswered `active: not yet` — \"the next prompt applies it\" — forever.\nNothing else in the report could tell that state from a genuine first\nprompt.\n\nDeliberately **not** a [`Reason`] arm: the consent predicate did not\nrefuse, so this is not one of its eight enumerated answers."
        },
        "carrier_present": {
          "type": "boolean",
          "description": "Whether the `__OCX_ENV_STATE` carrier is set at all.\n\nThis is what separates the two halves of C-050 reason 6: an absent\ncarrier is the first prompt of a shell (nothing applied, nothing to\nrepair); a carrier that is present and undecodable is a corrupt one (a\nscope was applied and its record is gone)."
        },
        "carrier_bytes": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "The carrier's encoded length in bytes, against [`MAX_CARRIER_BYTES`]."
        },
        "ledger": {
          "anyOf": [
            {
              "$ref": "#/$defs/Ledger"
            },
            {
              "type": "null"
            }
          ],
          "description": "The decoded ledger, **rendered as fields, never as base64** — envelope\ntag, schema `v`, and the payload. `None` when the carrier is absent or\n[`Ledger::decode`] refused it (C-003, C-006).\n\nCarries what is applied per scope (`global` and `project` separately),\nwhether `priors` are intact for each constant the project scope owns —\nthe one datum nothing can reconstruct — and, through `over_cap`, the\nabandoned-scope marker (A-01: read from the marker, never inferred from\nan absent carrier)."
        },
        "fingerprint_current": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "Whether the ledger's recorded `fp` still matches the watch set as it\nstands on disk right now (C-019). `None` when there is no recorded\nfingerprint to compare against, **and** when no fold is available to\ncompare with: the fingerprint is the reconciler's\n([`ocx_lib::shell::reconcile::fingerprint`], which folds the raw\n`OCX_CONSENT_*` values, the recorded config-tier paths and the project's\nconsent stamp — A-13), and a second fold defined here would produce a\ndifferent string and report every fresh shell as stale."
        },
        "watch_set": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/WatchMember"
          },
          "description": "The watch set, with each member's presence, size and mtime (C-050)."
        },
        "project_dir": {
          "type": [
            "string",
            "null"
          ],
          "description": "The project the CWD walk resolved, canonicalized (A-30), and its\n16-hex state key."
        },
        "project_key": {
          "type": [
            "string",
            "null"
          ],
          "description": "`ReferenceManager::name_for_path` of [`Self::project_dir`]."
        },
        "project_stamped": {
          "type": "boolean",
          "description": "Whether a usable consent stamp exists for that key (A-25: an unusable\nstamp is an absent stamp)."
        },
        "grant": {
          "anyOf": [
            {
              "$ref": "#/$defs/Grant"
            },
            {
              "type": "null"
            }
          ],
          "description": "**Which clause activated this project**, or `None` when it is inert.\n\nThe consent stamp is an independent grant kind that outranks the\n`[shell.consent]` table a user reads, and until this field existed the\nreport could not tell the two apart: a project stamped by a past\n`ocx add` looked exactly like one the config granted, so the config and\nthe behaviour disagreed with nothing to say so. Naming the granting\nclause is the whole fix — a user who sees `stamp` where they expected\n`paths` now knows which one to revoke.\n\nSerialized as the [`Grant`] discriminant: `\"stamp\"`, `\"namespace\"` or\n`\"path\"`."
        },
        "stamp_written_at": {
          "type": [
            "string",
            "null"
          ],
          "description": "When the consent stamp was written (RFC 3339, UTC), when there is one.\n\nThe stamp's own recorded instant, not a `stat` — a stamp replaced in\nplace would otherwise report the moment of the replacement. `None`\nwhenever [`Self::project_stamped`] is `false`; the two come from one\nread, so they cannot disagree."
        },
        "priors": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/PriorStatus"
          },
          "description": "Prior intactness for each constant the ledger's project scope owns."
        },
        "hook": {
          "$ref": "#/$defs/HookStatus",
          "description": "The hook's enablement and the rung that decided it (C-038)."
        },
        "yielded_to": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Observation"
          },
          "description": "Every coexisting tool observed live in this shell (C-049).\n\nA-37 — the two sentinels fire independently, so this is a list and the\nrenderer prints **one line per observed tool**. [`Reason::YieldedTo`]\ncarries a single [`Observation`], which is why the enumerated reason\nnames the first and this field carries all of them."
        },
        "inert_reason": {
          "anyOf": [
            {
              "$ref": "#/$defs/Reason"
            },
            {
              "type": "null"
            }
          ],
          "description": "Why the shell is not active, when it is not — **the command's reason to\nexist**. `None` when the project is active."
        },
        "notes": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Note"
          },
          "description": "Reason rows that explain an answer without being an inertness verdict of\ntheir own (A-12, A-26, A-28)."
        }
      },
      "required": [
        "ocx_home",
        "ocx_home_present",
        "shell_integration_installed",
        "lock_refusal",
        "carrier_present",
        "carrier_bytes",
        "ledger",
        "fingerprint_current",
        "watch_set",
        "project_dir",
        "project_key",
        "project_stamped",
        "grant",
        "stamp_written_at",
        "priors",
        "hook",
        "yielded_to",
        "inert_reason",
        "notes"
      ],
      "description": "What `ocx shell state` reports — all derived, none of it mutating.\n\nRead-only, absolutely: it never writes a stamp, never repairs a ledger,\nnever emits a plan (A-29 names it a non-member of the six-writer stamp\nallowlist — a stamp written from here would consent to the very project it\nis diagnosing). Repair is the `unset __OCX_ENV_STATE` gesture or a new\nshell (C-012); this command is how a user checks the gesture worked."
    },
    "Ledger": {
      "type": "object",
      "properties": {
        "v": {
          "type": "integer",
          "format": "uint8",
          "minimum": 0,
          "maximum": 255,
          "description": "Schema version of the payload shape."
        },
        "fp": {
          "type": "string",
          "description": "Watch-set fingerprint (C-019). A-13 folds the raw `OCX_CONSENT_*`\nvalues, the recorded config-tier paths and the project's consent stamp\ninto it, which is what makes `verdict` expirable."
        },
        "verdict": {
          "anyOf": [
            {
              "$ref": "#/$defs/Verdict"
            },
            {
              "type": "null"
            }
          ],
          "description": "The cached negative verdict; [`Verdict::Inert`] or\n[`Verdict::NoProject`], never [`Verdict::Activate`]."
        },
        "tiers": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The config-tier paths that were in effect at compose time (A-13).\n\nRecorded rather than re-derived, and that distinction is the whole\npoint: the `--config` / `OCX_CONFIG` explicit tier is a\n**consent-bearing channel** (A-33), but the emitted hook body invokes\n`--reconcile` with no `--config`, so a per-prompt process re-deriving\nthe list can never see it — and a grant added to that file would never\nexpire the cached `inert` verdict. Seeded by the shell-start\n`ConfigLoader` pass, which is the one run that knows it, and carried\nforward unchanged from there.\n\nAdditive (A-04): optional on the wire, so it moves neither `v` nor the\nenvelope tag, and an absent list simply falls back to re-derivation."
        },
        "ws": {
          "type": "string",
          "description": "The **membership** of the watch set baked into the shell's emitted gate\n([ocx-sh/ocx#347](https://github.com/ocx-sh/ocx/issues/347)).\n\nNot a second [`Ledger::fp`]. `fp` folds every member's presence, size and\nmtime and answers *\"did anything move?\"*; this folds the ordered path\nlist alone and answers *\"is the shell watching the right files?\"*. The\ntwo are independent: a project's `ocx.lock` can move `fp` on every edit\nwhile `ws` never budges, and entering a project moves both.\n\nIt exists because the gate is **baked at emission time**, not read per\nprompt: [`registration`] writes one newer-than term per watch path into\nthe hook body, and that list then decides whether ocx is invoked at all.\nA reconcile recomputes the watch set every prompt, but the shell keeps\ngating on the list it was given — so a project entered mid-session was\ncomposed correctly and then never noticed again, and an `ocx add` inside\nit could not reach the next prompt.\n\nWritten by, and only by, the emission that redefines the gate\n([`redefinition`]); a value that ran ahead of the emission would describe\na gate the shell does not have. [`next_ledger`] therefore carries it\nforward unchanged, exactly as it does [`Ledger::tiers`].\n\nAdditive (A-04): optional on the wire, so it moves neither `v` nor the\nenvelope tag. A carrier written by a binary that did not know the field\ndecodes as empty, which no real membership digest equals — one redundant\nredefinition, never a missed one, which is the fail-safe direction.\n\n[`registration`]: crate::shell::hook::registration\n[`redefinition`]: crate::shell::hook::redefinition\n[`next_ledger`]: crate::activation::next_ledger"
        },
        "messages_fp": {
          "type": "string",
          "description": "A digest of the deferred diagnostics the previous prompt printed (A-21).\n\nDeliberately **not** the messages themselves. The only question the next\nprompt asks is *\"is this line new\"*, and 16 hex characters answer it in\nthe carrier budget the direnv-yield line alone would spend 70 bytes of —\nthe `[env]` hint another 150.\n\nWithout it, every message in [`Outcome::messages`] re-prints on **every**\nprompt for as long as its cause holds. For a directory direnv manages\nthat is the shell's whole life, and neither of the two verdicts a message\nrides with is cacheable: an `Activate` verdict never is (C-007), and a\nyield hangs off an env sentinel `fp` does not fold. The summary line\nsolved the same problem for itself by being a delta against the ledger;\nthis is the rest of A-21's output getting the same treatment, in the same\nshape as `over_cap`'s \"already announced on the prompt that did it\" rule.\n\nAdditive (A-04): optional on the wire, so it moves neither `v` nor the\nenvelope tag, and a binary that does not know it re-prints once. It\nsurvives the over-cap marker alongside `fp`, or an over-cap shell would\nre-announce on every prompt for exactly the reason this field exists.\n\n[`Outcome::messages`]: crate::activation::Outcome::messages"
        },
        "over_cap": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ScopeId"
          },
          "description": "Scopes the 16 KiB cap dropped (C-004, A-01). An additive field on this\nschema — it bumps neither `v` nor the envelope tag — and a scope it\nnames is reconciled exactly as an absent scope. Omitted when empty."
        },
        "scopes": {
          "$ref": "#/$defs/Scopes",
          "description": "What each scope applied."
        }
      },
      "required": [
        "v",
        "fp",
        "verdict",
        "scopes"
      ],
      "description": "The decoded payload of `__OCX_ENV_STATE` (C-002).\n\n`v` describes **shape**; the envelope tag describes **encoding** (C-003). A\nchange that is both bumps both. A-04 — `v` is additive-only, and a shape\nbreak ships a `v-1` revert-read arm in the same release."
    },
    "Verdict": {
      "oneOf": [
        {
          "type": "string",
          "const": "activate",
          "description": "Never written to the carrier; present so the enum is total."
        },
        {
          "type": "string",
          "const": "inert",
          "description": "The negative-consent cache, expired by the watch set (C-019, C-042)."
        },
        {
          "type": "string",
          "const": "noproject",
          "description": "The walk resolved no project at all. Not consent-derived — there is no\nproject to consent to — so caching it leaves C-007 untouched;\n`project_dir` is folded into the fingerprint, so entering any project\nexpires it.\n\nDistinct from [`Verdict::Inert`] rather than folded into it: `inert`\nmeans *a project was resolved and consent refused it*, which\n`ocx shell state` reports and which a later grant must expire through the\nproject's consent stamp — a stamp there is no key for when no project was\nresolved. Overloading one variant would make the two indistinguishable in\nthe carrier and in that report.\n\nWire spelling `\"noproject\"`. A binary predating this variant fails\n[`Ledger::decode`] on it and treats the ledger as absent (C-006, the\nfail-safe direction) — reachable only across a `self update` mid-session,\nand the very next prompt re-derives everything from scratch."
        }
      ],
      "description": "The cached activation verdict (C-002).\n\n**No positive verdict is ever written.** An `Activate` verdict is re-derived\nevery prompt and never read back from the carrier — caching it would make the\nledger a consent input, which C-007 forbids. The variant exists so the\nvocabulary is total and the wire value is checkable. The two cached verdicts\nare both *negative*: they can only ever cause ocx to do less, which is the\nfail-safe direction, and the watch set expires both (A-13)."
    },
    "ScopeId": {
      "oneOf": [
        {
          "type": "string",
          "const": "global",
          "description": "The `--global` toolchain tier."
        },
        {
          "type": "string",
          "const": "project",
          "description": "The project resolved by the CWD walk."
        }
      ],
      "description": "Which scope a ledger datum belongs to. Wire spelling `global` / `project`.\n\nC-018 — exactly two slots. A project nested inside a project does not layer;\nthe inner one *replaces* the outer, so moving between them is a switch."
    },
    "Scopes": {
      "type": "object",
      "properties": {
        "global": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/LedgerEntry"
          },
          "description": "The global toolchain tier's applied entries."
        },
        "global_priors": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/Prior"
          },
          "description": "Pre-apply values for the **global** scope's constants (R1).\n\nA **sibling** field rather than a `priors` member inside `global`,\nbecause turning that JSON array into an object would fail every live\ncarrier's decode — the fleet-wide `priors` loss A-04 exists to forbid.\nAs an optional additive field it bumps neither `v` nor the envelope tag\n(A-04), and an older binary simply ignores it.\n\nThe design spec justified having no global priors with \"the global tier\nis the user's own file and is never *left*\". That conflates *the scope is\nnever exited* with *a key is never removed from it*, and\n`ocx remove --global <pkg>` removes keys: without this map a retired\nglobal constant had no prior to restore and ocx's value stayed in the\nshell for its whole life, and a project constant shadowing a global one\nrestored **global's** value on a two-scope retirement — the project prior\nis captured after global applied (C-018), so that is what it holds.\n\nCaptured against the **pre-global** environment, which only the ledger's\nproducer sees."
        },
        "project": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProjectScope"
            },
            {
              "type": "null"
            }
          ],
          "description": "The resolved project tier."
        }
      },
      "description": "The two scope slots (C-018).",
      "required": [
        "global",
        "project"
      ]
    },
    "LedgerEntry": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string",
          "description": "Environment-variable name."
        },
        "value": {
          "type": "string",
          "description": "The exact string ocx wrote, byte for byte."
        },
        "type": {
          "$ref": "#/$defs/ModifierKind",
          "description": "How the value combines — re-derived from D for every key D declares\n(C-007 rule (b)); L's copy is used only for the revert set."
        },
        "separator": {
          "type": [
            "string",
            "null"
          ],
          "description": "The effective list separator; `None` is path-kind only (A-08)."
        }
      },
      "required": [
        "key",
        "value",
        "type",
        "separator"
      ],
      "description": "One env-var binding the reconciler applied, recorded literally.\n\nC-001 — the wire field is **`type`, never `kind`**: the spelling\n`ocx_cli::api::data::env::EnvEntry` already emits and the nushell shim\nalready parses. Values are raw and unescaped (C-009, invariant L-2) and\nbyte-exact copies of what ocx wrote (C-008, invariant L-1).\n\nA-08 — `separator` holds the **effective** separator, resolved once at\nrecord time: always `Some` for [`ModifierKind::List`] (defaulting to\n[`DEFAULT_SEPARATOR`]), and `None` reserved for path-kind, where it means\n[`crate::env::PATH_SEPARATOR`]."
    },
    "Prior": {
      "oneOf": [
        {
          "type": "string",
          "const": "unset",
          "description": "The variable did not exist before ocx set it; reverting removes it."
        },
        {
          "type": "object",
          "properties": {
            "value": {
              "type": "string"
            }
          },
          "required": [
            "value"
          ],
          "additionalProperties": false,
          "description": "The variable held this exact string; reverting restores it."
        }
      ],
      "description": "What a scope's previous value was, for the constant revert path (C-015).\n\nA-05 — capture reads set-ness through `std::env::var_os`, so a set-but-empty\nvariable is `Value(\"\")` and **never** `Unset`. Reverting `Value(\"\")` emits\nthat arm's `export_constant(key, \"\")`, never `Shell::unset`."
    },
    "ProjectScope": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string",
          "description": "`ReferenceManager::name_for_path` of the canonical project directory."
        },
        "dir": {
          "type": "string",
          "description": "The canonical project directory, advisory only."
        },
        "applied": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/LedgerEntry"
          },
          "description": "What this scope applied."
        },
        "priors": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/Prior"
          },
          "description": "Pre-apply constant values, captured against the **post-global**\nenvironment (C-018), so reverting the project leaves the global scope\nstanding.\n\nThat is also why a prior here can hold a value the *global* scope owns\nrather than the user's own — [`Ledger::prior`] hops to\n[`Scopes::global_priors`] when it does."
        }
      },
      "required": [
        "key",
        "dir",
        "applied",
        "priors"
      ],
      "description": "The project scope's record (C-002).\n\n`key` and `dir` are **advisory identity labels** (C-007 rule (a), A-03):\nboth are re-derived from the CWD walk every prompt, neither may construct a\npath, and `dir` never gates a revert — any value other than the walk's own\nresult means the scope has been left.\n\n# The one exception\n\nA-11's determinacy probe\n([`walk_is_indeterminate`](crate::activation::walk_is_indeterminate)) is the\nsingle sanctioned use of `dir` as a path, and it is a *retention* of the\nalready-applied scope rather than a revert — the fail-safe direction. It is\nbounded three ways: `dir` must be absolute (A-30 makes an honest one\ncanonical), it must be an ancestor-or-self of the live CWD, and the only\neffect is one `symlink_metadata` on `<dir>/ocx.toml`. So a carrier can name\nno path outside the process's own working-directory ancestry, and still\nselects no value, re-grants no consent, and names no revert.\n\nEverywhere else the rule above holds verbatim."
    },
    "WatchMember": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "The watched path."
        },
        "present": {
          "type": "boolean",
          "description": "Whether it exists right now."
        },
        "size": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0,
          "description": "Size in bytes, when present."
        },
        "mtime": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0,
          "description": "Modification time as whole seconds since the Unix epoch, when present —\nthe same granularity the per-prompt fast path compares."
        }
      },
      "required": [
        "path",
        "present",
        "size",
        "mtime"
      ],
      "description": "One member of the fingerprint watch set (C-019, A-13), as it stands on disk\nright now.\n\nAbsent members are recorded too: a tier file that did not exist becoming\npresent is exactly the change the watch set must notice, which is why the\nloader records candidates rather than survivors (A-13)."
    },
    "Grant": {
      "oneOf": [
        {
          "type": "string",
          "const": "stamp",
          "description": "Clause 1 — a valid stamp for this directory whose source set covers the\nlock's. A human ran one of the six stamp-writing commands here."
        },
        {
          "type": "string",
          "const": "namespace",
          "description": "Clause 2 — every repository the store recorded for the lock's tools\nresolves to a source matching `[shell.consent] namespaces`. A fleet-wide\nauto-enabler, bounded by what this host actually pulled under that name\nrather than by what the lock claims."
        },
        {
          "type": "string",
          "const": "path",
          "description": "Clause 3 — the canonical directory is covered by `[shell.consent]\npaths`: by an entry naming it exactly, or by a trailing-`/*` entry\nnaming it or an ancestor of it and granting that whole subtree. An\noperator or the user wrote this directory — or a tree holding it —\ndown, which is why this grant opens the project `[env]` channel for a\ncheckout the entry never named."
        }
      ],
      "description": "Which clause of [`evaluate_with_stamp`] granted, and therefore **how much**\nit granted.\n\nThe distinction is a trust boundary, not bookkeeping. `ocx.lock` is text the\nproject itself authors: an attacker writes `ocx.sh/<granted-org>/anything`\ninto a lock, and the named package need not exist, be pullable or be signed.\n**That text authorizes nothing on its own.** Clause 2 therefore does not\nquantify over the lock's claim at all — it quantifies over\n[`verified_sources`], the store's own record of which logical repository\nthis host resolved and materialized digest-verified content for\n(`refs/origins/`). The claim is satisfiable by text a clone's author writes;\nthe record only by an act of pulling on this host under that name. That is\nwhere the record is evidence and the claim is not — and it is the whole of\nthe difference, no more: on a cold store the pull must fetch the bytes over\nthe wire under that name, which needs that namespace's publish credential,\nbut on a warm one it need not. See [`verified_sources`] for the write gate\nas shipped and for the residual that leaves.\n\nThat record exists only for the **package** channel. The project-file\n`[env]` channel has no publisher at all — a relative `type = \"path\"` value\nresolves against the project root, so one line of `ocx.toml` puts\n`<clone>/bin` in front of `PATH` — and it is therefore authorized only by\nclauses 1 and 3, the two clauses that stand on a human's own gesture rather\nthan on text a clone ships.\n\n**Clause 3's gesture names a directory or a tree, and a tree is still the\ngesture.** A `paths` entry is one exact directory, or — written with a\ntrailing `/*` — that directory and everything beneath it\n([`consent_path_matches`]). A subtree entry therefore opens the `[env]`\nchannel for every project under it, **including ones that do not exist\nyet**: a clone dropped into a granted tree tomorrow activates on its first\nprompt, its own `ocx.toml` `[env]` included. That is the deliberate reading\nof the form, not a leak it tolerates — it is the devcontainer and CI-image\ncase the form exists for, where the workspace root is written down once in\nan image and the checkouts arrive later. It is the reach `git`'s own\n`safe.directory = /w/acme/*` has, widened by one directory: git's form\ncovers only what is nested *under* the named directory, ours covers that\ndirectory too (measured, git 2.54). The operator's gesture remains\nthe whole bound: it is spelled in a `config.toml` tier or `OCX_CONSENT_PATHS`\nand never in project bytes, it is component-bounded so a sibling\n`/w/acme-evil` is outside it, and no `*` spelling reaches the filesystem\nroot. What clause 3 does not do is *narrow* to a leaf — an operator who\nmeans one directory writes one directory.\n\nThe near-exact precedent is mise's trust bypass, CVE-2026-35533 /\nGHSA-436v-8fw5-4mj8. The borrowed-digest variant clause 2 used to admit is\n[ocx-sh/ocx#344](https://github.com/ocx-sh/ocx/issues/344)."
    },
    "PriorStatus": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string",
          "description": "The constant's env key, as the carrier records it."
        },
        "intact": {
          "type": "boolean",
          "description": "Whether a [`Prior`] is still recorded for it."
        }
      },
      "required": [
        "key",
        "intact"
      ],
      "description": "Whether the project scope still holds the prior for one constant it owns.\n\nThe priors are the one datum nothing can reconstruct (C-050), and the thing\nC-012's `unset __OCX_ENV_STATE` repair gesture destroys — which is why this\nis reported per constant rather than as a single yes/no."
    },
    "HookStatus": {
      "type": "object",
      "properties": {
        "rung": {
          "type": "string",
          "description": "The deciding rung, rendered the way a user spells it."
        },
        "tier": {
          "type": [
            "string",
            "null"
          ],
          "description": "The config tier that set it, when rung 4 decided (A-32 — the tier that\n**actually** decided, never a hard-coded \"managed\")."
        },
        "enabled": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "The resolved answer, or `None` on rung 5: \"auto\" is decided shell-side\nby the shim's interactivity probe, which a diagnostic cannot observe."
        }
      },
      "required": [
        "rung",
        "tier",
        "enabled"
      ],
      "description": "The per-prompt hook's enablement, read from C-038's ladder rather than\nre-derived.\n\n`ocx shell state` declares no `--hook` / `--no-hook` pair of its own, so\nrungs 1 and 2 are unreachable from here by construction and the answer comes\nfrom rung 3 (`OCX_NO_HOOK`), rung 4 (`[shell] hook`) or rung 5 (auto)."
    },
    "Observation": {
      "type": "object",
      "properties": {
        "tool": {
          "$ref": "#/$defs/Tool",
          "description": "The tool observed live in this shell."
        },
        "signal": {
          "type": "string",
          "description": "The env-var evidence, rendered for a human."
        }
      },
      "required": [
        "tool",
        "signal"
      ],
      "description": "One live-session observation: which tool, and the signal that proved it.\n\n`signal` is what `ocx shell state` renders, because a user staring at an\n`.envrc` will guess the wrong cause (C-050 reason 4) — it names the variable\nobserved and, for direnv, the directory it named."
    },
    "Tool": {
      "oneOf": [
        {
          "type": "string",
          "const": "direnv",
          "description": "Yielded on `DIRENV_DIR` **naming the resolved project's canonical\ndirectory**. A `DIRENV_DIR` naming a *different* directory is treated as\nabsent — direnv is active for some ancestor, not for this project."
        },
        {
          "type": "string",
          "const": "mise",
          "description": "Yielded on `MISE_SHELL` **or** `__MISE_ORIG_PATH` being present."
        }
      ],
      "description": "A coexisting per-prompt environment manager."
    },
    "Reason": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "derived_sources": {
              "type": "array",
              "uniqueItems": true,
              "items": {
                "type": "string"
              },
              "description": "The source set derived from `ocx.lock` (C-026)."
            },
            "paths_tested": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "The `paths` entries compared against the canonical directory."
            },
            "namespaces_tested": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "The `namespaces` patterns the derived sources were matched against."
            },
            "reason": {
              "type": "string",
              "const": "no_stamp_no_grant"
            }
          },
          "required": [
            "derived_sources",
            "paths_tested",
            "namespaces_tested",
            "reason"
          ],
          "description": "No valid stamp and no matching grant — with the project's derived source\nset and the grants it was tested against, so the user can see what to\nadd. A-28 also surfaces a `paths` **near-miss** here: an entry differing\nfrom the canonical directory only by ASCII case."
        },
        {
          "type": "object",
          "properties": {
            "new_sources": {
              "type": "array",
              "uniqueItems": true,
              "items": {
                "type": "string"
              },
              "description": "Sources present in the lock and absent from the stamp."
            },
            "reason": {
              "type": "string",
              "const": "source_set_drift"
            }
          },
          "required": [
            "new_sources",
            "reason"
          ],
          "description": "A stamp exists but the current lock's source set is not a subset of it —\n**naming the source that is new**."
        },
        {
          "type": "object",
          "properties": {
            "claimed_sources": {
              "type": "array",
              "uniqueItems": true,
              "items": {
                "type": "string"
              },
              "description": "The source set derived from `ocx.lock`'s repository fields (C-026)."
            },
            "verified_sources": {
              "type": [
                "array",
                "null"
              ],
              "uniqueItems": true,
              "items": {
                "type": "string"
              },
              "description": "The source set derived from the store's recorded pull origins, or\n`None` when no complete record exists."
            },
            "reason": {
              "type": "string",
              "const": "uncorroborated_namespace"
            }
          },
          "required": [
            "claimed_sources",
            "verified_sources",
            "reason"
          ],
          "description": "Every source the lock *claims* matches `[shell.consent] namespaces`, but\nthe package store's own record of where the locked digests came from\ndoes not corroborate the claim — so clause 2 refuses.\n\nThe two payloads are the whole diagnosis, and their difference is the\npoint: `claimed_sources` is what `ocx.lock` says, `verified_sources` is\nwhat `refs/origins/` proves. `None` there means nothing could be\nverified at all — a tool that resolves no host leaf, is not\nmaterialized, or predates origin recording — which is the ordinary\nfirst-encounter state and clears at the next `ocx pull`, *except* for\nthe one case that never clears: a digest already materialized under\nanother repository is a store hit, so no further pull ever mints a\nmarker for the repository this lock names (see [`verified_sources`]). A\n`Some` that disagrees is the interesting one: a digest in the store came\nfrom a repository outside the granted namespace."
        },
        {
          "type": "object",
          "properties": {
            "rung": {
              "type": "string",
              "description": "The deciding rung, rendered (`--no-hook`, `OCX_NO_HOOK`, `[shell] hook`, …)."
            },
            "tier": {
              "type": [
                "string",
                "null"
              ],
              "description": "The config tier that set it, when rung 4 decided."
            },
            "reason": {
              "type": "string",
              "const": "hook_disabled"
            }
          },
          "required": [
            "rung",
            "tier",
            "reason"
          ],
          "description": "The hook is disabled — naming which of C-038's five rungs decided it and\nthe tier that set it, including the managed tier winning over a user's\nown file (A-32: the tier that **actually** decided, never a hard-coded\n\"managed\").\n\nRendered names rather than typed values: the rung enum lives on\n`ocx_cli::options::hook::Hook` (C-038) and `ocx_lib` cannot name it."
        },
        {
          "$ref": "#/$defs/Observation",
          "type": "object",
          "properties": {
            "reason": {
              "type": "string",
              "const": "yielded_to"
            }
          },
          "required": [
            "reason"
          ],
          "description": "Yielded to another live per-prompt hook (C-049) — naming the **live\nsignal observed**, because a user staring at an `.envrc` will guess the\nwrong cause. A-37: one row per observed tool."
        },
        {
          "type": "object",
          "properties": {
            "scope": {
              "$ref": "#/$defs/ScopeId",
              "description": "The scope whose payload was dropped."
            },
            "reason": {
              "type": "string",
              "const": "ledger_over_cap"
            }
          },
          "required": [
            "scope",
            "reason"
          ],
          "description": "The ledger's payload exceeded the 16 KiB cap and this scope was\nabandoned — read from the `over_cap` **marker** the carrier still\ncarries (C-004, A-01), never inferred from an absent carrier. The one\ndegradation that loses information rather than repairing it."
        },
        {
          "type": "object",
          "properties": {
            "first_prompt": {
              "type": "boolean",
              "description": "`true` for the ordinary first-prompt absence."
            },
            "reason": {
              "type": "string",
              "const": "ledger_unreadable"
            }
          },
          "required": [
            "first_prompt",
            "reason"
          ],
          "description": "The carrier is absent, truncated, or carries an unrecognised envelope\ntag — **distinguishing** the first prompt of a shell (nothing applied,\nnothing to repair) from a corrupt carrier (a scope was applied and its\nrecord is gone). C-006 makes that distinction normative."
        },
        {
          "type": "object",
          "properties": {
            "reason": {
              "type": "string",
              "const": "lock_unavailable"
            }
          },
          "required": [
            "reason"
          ],
          "description": "`ocx.lock` is absent, unreadable or unparseable — all three share one\noutcome, because all three leave the source-set predicate with nothing\nto quantify over."
        }
      ],
      "description": "Why a shell is not active — the enumerated set `ocx shell state` renders.\n\nThis enumeration is `ocx shell state`'s reason to exist (C-050): each\nvariant must be individually reachable and individually tested.\n\n[`Reason::HookDisabled`] and [`Reason::YieldedTo`] are decided *outside*\n[`evaluate`] and constructed by the deciding call site — Decision 10\nenumerates them in one list as \"the reason the shell is not active\", and a\nsingle enum is what makes the report total."
    },
    "Note": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "candidate": {
              "type": "string",
              "description": "The symlinked candidate that was skipped."
            },
            "ancestor": {
              "type": "string",
              "description": "The ancestor project activated in its place."
            },
            "note": {
              "type": "string",
              "const": "symlinked_candidate_skipped"
            }
          },
          "required": [
            "candidate",
            "ancestor",
            "note"
          ],
          "description": "A-12 — the CWD walk skipped a symlinked `ocx.toml` candidate and\npromoted an ancestor. The loader's `log::warn!` never reaches the prompt\n(the hook discards the binary's stderr unconditionally, A-21), so this\nrow is the user's only path to that answer."
        },
        {
          "type": "object",
          "properties": {
            "entry": {
              "type": "string",
              "description": "The granting entry."
            },
            "note": {
              "type": "string",
              "const": "active_via_paths_grant"
            }
          },
          "required": [
            "entry",
            "note"
          ],
          "description": "A-26 — active via a `paths` grant, which is unconditional: source-set\ndrift is **not** tracked for path grants, because nothing on the\nactivation path writes a stamp to drift against."
        },
        {
          "type": "object",
          "properties": {
            "detail": {
              "type": "string",
              "description": "The resolution failure, rendered for a human."
            },
            "note": {
              "type": "string",
              "const": "project_unresolved"
            }
          },
          "required": [
            "detail",
            "note"
          ],
          "description": "QUAL-3 — a project file was reachable but could not be resolved (an\nunparseable `ocx.toml`, a canonicalization failure). Without this row\nthe report says *\"no project reachable from this directory\"*, which is\nfalse for a user standing in a project whose config is broken — and this\nis the command whose product is the explanation."
        },
        {
          "type": "object",
          "properties": {
            "entry": {
              "type": "string",
              "description": "The entry that nearly matched."
            },
            "canonical": {
              "type": "string",
              "description": "The canonical project directory it was compared against."
            },
            "note": {
              "type": "string",
              "const": "paths_near_miss"
            }
          },
          "required": [
            "entry",
            "canonical",
            "note"
          ],
          "description": "A-28 — a `paths` entry that would grant `canonical` if the compare\nfolded ASCII case, exact or subtree entry alike. Entries are compared\nas literal bytes ([`ocx_lib::consent_path_matches`]), so this is\n`Inert`; the row exists to pay off the support cost of that decision."
        },
        {
          "type": "object",
          "properties": {
            "entry": {
              "type": "string",
              "description": "The malformed entry."
            },
            "defect": {
              "type": "string",
              "description": "[`ocx_lib::EntryDefect`]'s own rendering of what is wrong with it."
            },
            "note": {
              "type": "string",
              "const": "paths_defect"
            }
          },
          "required": [
            "entry",
            "defect",
            "note"
          ],
          "description": "A `[shell.consent] paths` entry that can never match any project by\nconstruction — [`ocx_lib::consent_entry_defect`]'s classification of\nthe entry's own bytes, independent of which project is in view.\n`namespaces`' sibling grant refuses a pattern this broken at parse\ntime (A-27); a `paths` entry is an ordinary path and parses\nregardless of whether it can ever grant, so without this row it sits\nin the config forever, granting nothing and saying nothing."
        }
      ],
      "description": "A reason row that is not a member of [`Reason`] because it does not make the\nshell inert on its own — it explains an answer the user would otherwise get\nwrong."
    },
    "VerboseShellState": {
      "type": "object",
      "properties": {
        "ocx_home": {
          "type": "string",
          "description": "`$OCX_HOME`, and whether it exists. A missing home is an ordinary state\non a fresh install and exits 0; only a home that cannot be *read* is the\nsingle 74 path (C-051)."
        },
        "ocx_home_present": {
          "type": "boolean",
          "description": "Whether `$OCX_HOME` exists on disk."
        },
        "shell_integration_installed": {
          "type": "boolean",
          "description": "Whether `ocx self setup` has ever wired this machine's shell — probed\nas [`ocx_lib::setup::shims::WITNESS_SHIM`] under [`Self::ocx_home`].\n\nC-050 reason 6's first-prompt half tells the user \"the next prompt\napplies it\". That is true only once the rc/profile fence exists to\nsource the shim, and on a bare-binary install neither does. Without\nthis field the report answers the command's single most likely first\nuse — *\"I installed ocx, why isn't it working\"* — with a fix that can\nnever come true, so the renderer branches on it rather than promising\nconvergence that nothing will perform.\n\nDeliberately **not** a [`Reason`] arm: \"setup has not run\" is not a\nconsent state, and `consent::Reason` enumerates consent states."
        },
        "lock_refusal": {
          "type": [
            "string",
            "null"
          ],
          "description": "Why the project's `ocx.lock` refuses composition, when it does — the\n`Display` of [`ocx_lib::project::LockCurrency`], rendered as text the\nsame way [`Note::ProjectUnresolved`] carries its detail.\n\nConsent can say *activate* over a lock that composition then refuses: a\n`paths` grant holds without a readable lock at all, and a stale one\nstill parses. Every prompt then exits 65 with a stderr the hook\ndiscards (A-21), so the scope never reaches the ledger and the report\nanswered `active: not yet` — \"the next prompt applies it\" — forever.\nNothing else in the report could tell that state from a genuine first\nprompt.\n\nDeliberately **not** a [`Reason`] arm: the consent predicate did not\nrefuse, so this is not one of its eight enumerated answers."
        },
        "carrier_present": {
          "type": "boolean",
          "description": "Whether the `__OCX_ENV_STATE` carrier is set at all.\n\nThis is what separates the two halves of C-050 reason 6: an absent\ncarrier is the first prompt of a shell (nothing applied, nothing to\nrepair); a carrier that is present and undecodable is a corrupt one (a\nscope was applied and its record is gone)."
        },
        "carrier_bytes": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "The carrier's encoded length in bytes, against [`MAX_CARRIER_BYTES`]."
        },
        "ledger": {
          "anyOf": [
            {
              "$ref": "#/$defs/Ledger"
            },
            {
              "type": "null"
            }
          ],
          "description": "The decoded ledger, **rendered as fields, never as base64** — envelope\ntag, schema `v`, and the payload. `None` when the carrier is absent or\n[`Ledger::decode`] refused it (C-003, C-006).\n\nCarries what is applied per scope (`global` and `project` separately),\nwhether `priors` are intact for each constant the project scope owns —\nthe one datum nothing can reconstruct — and, through `over_cap`, the\nabandoned-scope marker (A-01: read from the marker, never inferred from\nan absent carrier)."
        },
        "fingerprint_current": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "Whether the ledger's recorded `fp` still matches the watch set as it\nstands on disk right now (C-019). `None` when there is no recorded\nfingerprint to compare against, **and** when no fold is available to\ncompare with: the fingerprint is the reconciler's\n([`ocx_lib::shell::reconcile::fingerprint`], which folds the raw\n`OCX_CONSENT_*` values, the recorded config-tier paths and the project's\nconsent stamp — A-13), and a second fold defined here would produce a\ndifferent string and report every fresh shell as stale."
        },
        "watch_set": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/WatchMember"
          },
          "description": "The watch set, with each member's presence, size and mtime (C-050)."
        },
        "project_dir": {
          "type": [
            "string",
            "null"
          ],
          "description": "The project the CWD walk resolved, canonicalized (A-30), and its\n16-hex state key."
        },
        "project_key": {
          "type": [
            "string",
            "null"
          ],
          "description": "`ReferenceManager::name_for_path` of [`Self::project_dir`]."
        },
        "project_stamped": {
          "type": "boolean",
          "description": "Whether a usable consent stamp exists for that key (A-25: an unusable\nstamp is an absent stamp)."
        },
        "grant": {
          "anyOf": [
            {
              "$ref": "#/$defs/Grant"
            },
            {
              "type": "null"
            }
          ],
          "description": "**Which clause activated this project**, or `None` when it is inert.\n\nThe consent stamp is an independent grant kind that outranks the\n`[shell.consent]` table a user reads, and until this field existed the\nreport could not tell the two apart: a project stamped by a past\n`ocx add` looked exactly like one the config granted, so the config and\nthe behaviour disagreed with nothing to say so. Naming the granting\nclause is the whole fix — a user who sees `stamp` where they expected\n`paths` now knows which one to revoke.\n\nSerialized as the [`Grant`] discriminant: `\"stamp\"`, `\"namespace\"` or\n`\"path\"`."
        },
        "stamp_written_at": {
          "type": [
            "string",
            "null"
          ],
          "description": "When the consent stamp was written (RFC 3339, UTC), when there is one.\n\nThe stamp's own recorded instant, not a `stat` — a stamp replaced in\nplace would otherwise report the moment of the replacement. `None`\nwhenever [`Self::project_stamped`] is `false`; the two come from one\nread, so they cannot disagree."
        },
        "priors": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/PriorStatus"
          },
          "description": "Prior intactness for each constant the ledger's project scope owns."
        },
        "hook": {
          "$ref": "#/$defs/HookStatus",
          "description": "The hook's enablement and the rung that decided it (C-038)."
        },
        "yielded_to": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Observation"
          },
          "description": "Every coexisting tool observed live in this shell (C-049).\n\nA-37 — the two sentinels fire independently, so this is a list and the\nrenderer prints **one line per observed tool**. [`Reason::YieldedTo`]\ncarries a single [`Observation`], which is why the enumerated reason\nnames the first and this field carries all of them."
        },
        "inert_reason": {
          "anyOf": [
            {
              "$ref": "#/$defs/Reason"
            },
            {
              "type": "null"
            }
          ],
          "description": "Why the shell is not active, when it is not — **the command's reason to\nexist**. `None` when the project is active."
        },
        "notes": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Note"
          },
          "description": "Reason rows that explain an answer without being an inertness verdict of\ntheir own (A-12, A-26, A-28)."
        }
      },
      "required": [
        "ocx_home",
        "ocx_home_present",
        "shell_integration_installed",
        "lock_refusal",
        "carrier_present",
        "carrier_bytes",
        "ledger",
        "fingerprint_current",
        "watch_set",
        "project_dir",
        "project_key",
        "project_stamped",
        "grant",
        "stamp_written_at",
        "priors",
        "hook",
        "yielded_to",
        "inert_reason",
        "notes"
      ],
      "description": "What `ocx shell state` reports — all derived, none of it mutating.\n\nRead-only, absolutely: it never writes a stamp, never repairs a ledger,\nnever emits a plan (A-29 names it a non-member of the six-writer stamp\nallowlist — a stamp written from here would consent to the very project it\nis diagnosing). Repair is the `unset __OCX_ENV_STATE` gesture or a new\nshell (C-012); this command is how a user checks the gesture worked."
    },
    "StatusReport": {
      "type": "object",
      "properties": {
        "project": {
          "type": "string",
          "description": "Absolute path of the `ocx.toml` this report describes."
        },
        "lock": {
          "$ref": "#/$defs/LockStatus"
        },
        "groups": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/GroupStatus"
          }
        },
        "package_settings": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/PackageSettingsOut"
          },
          "description": "`[package.\"<id>\"]` resolve-time policy, keyed by the canonical author\nstring. Reported here because it is excluded from `declaration_hash`,\nso nothing lock-derived can surface it."
        }
      },
      "required": [
        "project",
        "lock",
        "groups",
        "package_settings"
      ],
      "description": "Report emitted by `ocx status`.\n\nJSON format: `{ project, lock, groups, package_settings }`.\n\n`groups` and the maps inside it are keyed **objects**, not arrays: within one\nscope a binding name and an env key are unique by construction (they are TOML\ntable keys), and the underlying maps are key-sorted, so there is no authored\norder to lose and lookup-by-name is the natural access. `ocx inspect` uses\narrays for the opposite reason — a composed env can carry one key twice.\n\nPlain format: a tree (groups contain tools and env). Status holds the same\nsingle-table exemption `inspect` does — its content is inherently nested,\nand flattening groups × tools × env into one row shape would encode the\nstructure as string prefixes."
    },
    "LockStatus": {
      "type": "object",
      "properties": {
        "present": {
          "type": "boolean"
        },
        "error": {
          "type": "string",
          "description": "Why the lock could not be parsed. Its presence IS the unreadable state\n— no separate boolean, which could only ever repeat what this key\nalready says. The header fields and every binding's `platforms` are\nabsent alongside it: nothing was read."
        },
        "current": {
          "type": "boolean",
          "description": "`true` when the lock's stored `declaration_hash` matches the current\nconfig's. `false` means `ocx.toml` changed since the last `ocx lock`."
        },
        "lock_version": {
          "type": "integer",
          "format": "uint8",
          "minimum": 0,
          "maximum": 255
        },
        "declaration_hash": {
          "type": "string",
          "description": "The hash stored in `ocx.lock`."
        },
        "declaration_hash_expected": {
          "type": "string",
          "description": "The hash recomputed from `ocx.toml` — what the lock's stored hash would\nhave to be for `current` to hold. Emitted alongside the stored one so a\nconsumer sees *why* `current` is false rather than having to recompute\nthe project's canonicalization itself.\n\nNamed `expected`, not `current`: `current` one level up is a verdict\nabout the lock, and reusing the word for a hash value would give it two\nmeanings in one object.\n\nBoth hashes cover `[tools]` and `[group.*]` only — `[env]` and\n`[package.*]` are excluded by design, so an edit to either leaves\n`current` true."
        },
        "generated_by": {
          "type": "string"
        },
        "generated_at": {
          "type": "string"
        }
      },
      "required": [
        "present",
        "declaration_hash_expected"
      ],
      "description": "The `ocx.lock` header, plus whether the lock agrees with `ocx.toml`.\n\nEverything but `present` is absent when there is no lock — that is a valid\nstate (nothing has run `ocx lock` yet), not an error, so status reports it\nand exits 0. Same for a lock that exists but cannot be parsed (an\nunsupported `lock_version`, a corrupt file): `error` carries why, and the\nheader fields stay absent. Status is the command reached for when the\nproject is broken, so failing the way every other command already fails\nwould leave it with nothing to say in exactly that case."
    },
    "GroupStatus": {
      "type": "object",
      "properties": {
        "tools": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/ToolStatus"
          }
        },
        "env": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/EnvValueOut"
          },
          "description": "This scope's `[env]` table alone — never merged with another scope's.\nMerging is composition, and a merged view cannot show which scope\ndeclared a key."
        }
      },
      "required": [
        "tools",
        "env"
      ],
      "description": "One group's declarations. `default` is a group like any other: the\ntop-level `[tools]` and `[env]` tables in `ocx.toml` ARE its tools and env,\nwhich is why `default` is a reserved group name."
    },
    "ToolStatus": {
      "type": "object",
      "properties": {
        "declared": {
          "type": "string",
          "description": "The `ocx.toml` value for this binding, verbatim (`ocx.sh/go-task:3`)."
        },
        "platforms": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "EVERY platform leaf the lock records, not the host's. Picking the host\nleaf is resolution — `ocx inspect` does that."
        }
      },
      "description": "One tool binding, as the two files describe it.\n\nAbsence is the signal, matching the `binaries` convention in the inspect\nclosure (\"key absent on the wire means undeclared\"):\n\n- both keys — declared in `ocx.toml` and locked.\n- no `platforms` — declared but not yet locked (added since the last\n  `ocx lock`).\n- no `declared` — locked but no longer declared (orphaned in a stale lock)."
    },
    "EnvValueOut": {
      "type": "object",
      "properties": {
        "type": {
          "$ref": "#/$defs/ModifierKind"
        },
        "separator": {
          "type": "string",
          "description": "The declared separator for a `list`-typed value. `None` for every\nother kind, and for a `list` that declared none — the project surface\nmay omit it, and what the omission inherits is decided at compose\ntime, which status does not do. Skipped in JSON when `None`."
        },
        "value": {
          "type": "string",
          "description": "Verbatim as written. A relative `path` value stays relative — resolving\nit against the project root is composition, which `ocx inspect` and\n`ocx env` do."
        }
      },
      "required": [
        "type",
        "value"
      ],
      "description": "One declared environment value, normalized.\n\nDeliberately NOT `ProjectEnv`'s own `Serialize`: that one round-trips the\nTOML authoring grammar, emitting a constant as a bare string and a path as a\n`{ type, value }` table. A JSON consumer would have to branch on\nstring-vs-object per key to read it. The report always emits both fields."
    },
    "PackageSettingsOut": {
      "type": "object",
      "properties": {
        "no-patches": {
          "type": "boolean"
        }
      },
      "required": [
        "no-patches"
      ],
      "description": "Per-package resolve-time policy from `[package.\"<id>\"]`."
    },
    "SweepReport": {
      "type": "object",
      "properties": {
        "tags": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SweptTagReport"
          },
          "description": "One row per swept tag, in the order the tags were given."
        }
      },
      "required": [
        "tags"
      ],
      "description": "What a `--tags` / `--tags-file` sweep did, one row per swept tag.\n\nPlain format: a `Tag | Status | Detail` table, one row per tag in sweep\norder. JSON format:\n`{\"schema_version\":1,\"command\":\"<command>\",\"exit_code\":<code>,\n\"data\":{\"tags\":[{\"tag\",\"status\",\"report\"?,\"kind\"?,\"message\"?}]}}`.\n\nThe envelope's `exit_code` is the code the process returns, which for a\npartially-failed sweep is non-zero while the document still names every tag\nthat succeeded — the whole point of not aborting at the first failure."
    },
    "SweptTagReport": {
      "type": "object",
      "properties": {
        "tag": {
          "type": "string",
          "description": "The tag as the caller spelled it, so the report names what was asked\nfor rather than what it resolved to."
        },
        "status": {
          "$ref": "#/$defs/SweptStatus",
          "description": "What the sweep did to this tag."
        },
        "report": {
          "description": "The per-reference report, verbatim. Present for every tag whose run\nproduced one, which includes a `failed` row carrying a partial report:\na `--signature-format both` tag where one leg landed and one did not.",
          "$ref": "#/$defs/SignatureReport"
        },
        "kind": {
          "type": "string",
          "description": "The JSON error envelope's per-variant slug for this tag's failure,\nfalling back to its frozen category for errors outside the sign and\nverify taxonomies. Present exactly when `status` is `failed`.\n\nLifted out of the envelope this error would have rendered on its own,\nso the value a script reads here is the value it reads there — the same\nrule `push --sbom`'s failed attestation follows."
        },
        "message": {
          "type": "string",
          "description": "Human-readable cause, sanitized for the terminal (CWE-150). Present\nexactly when `status` is `failed`."
        }
      },
      "required": [
        "tag",
        "status"
      ],
      "description": "One swept tag's row.\n\nDeliberately flat rather than an internally-tagged enum: `report` is itself\na struct that would have to be `flatten`ed into the variant, and serde's\n`flatten` inside a tagged enum inside a `flatten` is a shape that silently\nchanges as those attributes compose. A `status` field plus three optionals\nis the same information with none of that."
    },
    "SweepReport2": {
      "type": "object",
      "properties": {
        "tags": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SweptTagReport2"
          },
          "description": "One row per swept tag, in the order the tags were given."
        }
      },
      "required": [
        "tags"
      ],
      "description": "What a `--tags` / `--tags-file` sweep did, one row per swept tag.\n\nPlain format: a `Tag | Status | Detail` table, one row per tag in sweep\norder. JSON format:\n`{\"schema_version\":1,\"command\":\"<command>\",\"exit_code\":<code>,\n\"data\":{\"tags\":[{\"tag\",\"status\",\"report\"?,\"kind\"?,\"message\"?}]}}`.\n\nThe envelope's `exit_code` is the code the process returns, which for a\npartially-failed sweep is non-zero while the document still names every tag\nthat succeeded — the whole point of not aborting at the first failure."
    },
    "SweptTagReport2": {
      "type": "object",
      "properties": {
        "tag": {
          "type": "string",
          "description": "The tag as the caller spelled it, so the report names what was asked\nfor rather than what it resolved to."
        },
        "status": {
          "$ref": "#/$defs/SweptStatus",
          "description": "What the sweep did to this tag."
        },
        "report": {
          "description": "The per-reference report, verbatim. Present for every tag whose run\nproduced one, which includes a `failed` row carrying a partial report:\na `--signature-format both` tag where one leg landed and one did not.",
          "$ref": "#/$defs/AttestationReport"
        },
        "kind": {
          "type": "string",
          "description": "The JSON error envelope's per-variant slug for this tag's failure,\nfalling back to its frozen category for errors outside the sign and\nverify taxonomies. Present exactly when `status` is `failed`.\n\nLifted out of the envelope this error would have rendered on its own,\nso the value a script reads here is the value it reads there — the same\nrule `push --sbom`'s failed attestation follows."
        },
        "message": {
          "type": "string",
          "description": "Human-readable cause, sanitized for the terminal (CWE-150). Present\nexactly when `status` is `failed`."
        }
      },
      "required": [
        "tag",
        "status"
      ],
      "description": "One swept tag's row.\n\nDeliberately flat rather than an internally-tagged enum: `report` is itself\na struct that would have to be `flatten`ed into the variant, and serde's\n`flatten` inside a tagged enum inside a `flatten` is a shape that silently\nchanges as those attributes compose. A `status` field plus three optionals\nis the same information with none of that."
    },
    "Tags": {
      "type": "object",
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      ],
      "description": "Tag listing for one or more packages, optionally including platform or variant details.\n\nPlain format: two-column table (Package | Tag) by default, or\n(Package | Platform) with `--platforms`, or (Package | Variant) with `--variants`.\n\nJSON format: object keyed by package name; values are arrays of tags, platforms, or variants."
    },
    "VerificationReport": {
      "type": "object",
      "properties": {
        "subject_digest": {
          "$ref": "#/$defs/Digest",
          "description": "Digest of the subject manifest whose signature was verified."
        },
        "referrer_digest": {
          "$ref": "#/$defs/Digest",
          "description": "What carried the verified signature — **not always a manifest**.\n\nThe OCI referrer manifest's digest for a Sigstore bundle; the **layer**\nblob's digest for a cosign simplesigning signature, whichever door found\nit, because there one layer is one signature and the manifest digest\nwould name all of them at once. The discriminator is\n`signatures[].signature_format`, never the discovery method — a\nsimplesigning sidecar reached through the Referrers API reports a layer\ndigest while `discovery_method` reads `referrers_api`. So this is\naddressable as `GET /v2/<name>/manifests/<digest>` only under\n`signature_format == \"bundle\"`."
        },
        "certificate_identity": {
          "type": "string",
          "description": "Certificate SAN (identity) embedded in the Fulcio cert."
        },
        "certificate_oidc_issuer": {
          "type": "string",
          "description": "Certificate OIDC issuer embedded in the Fulcio cert."
        },
        "signed_at": {
          "type": "string",
          "description": "Rekor integrated time (ISO-8601 UTC) of the signature entry."
        },
        "signatures": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SignatureEntry"
          },
          "description": "Every signature discovered for the subject.\n\n**Absent** while empty, rather than rendered as `[]`: a key that always\nrenders an empty array is a wire promise of behaviour that does not\nexist — a consumer reading `\"signatures\": []` learns \"we looked and\nfound none\", which is not what happened. Contrast `sbom[].shadowed`,\nwhich is emitted unconditionally because `false` there is a true\nstatement."
        }
      },
      "required": [
        "subject_digest",
        "referrer_digest",
        "certificate_identity",
        "certificate_oidc_issuer",
        "signed_at"
      ],
      "description": "Summary of a successful Sigstore verification.\n\nPlain format: single \"Field | Value\" table listing the subject digest,\nreferrer digest, cert identity, cert OIDC issuer, and signed-at timestamp.\n`subject_digest` is the answer (what was verified) and renders full;\n`referrer_digest` shortens to 12 hex — a full `sha256:<64hex>` earns its\nrow only once per view. Both stay full in JSON.\n\nJSON format: `{ subject_digest, referrer_digest, certificate_identity,\ncertificate_oidc_issuer, signed_at }`, plus `signatures` once a discovery\npipeline populates it."
    },
    "SignatureEntry": {
      "type": "object",
      "properties": {
        "signature_format": {
          "$ref": "#/$defs/SignatureFormat",
          "description": "Which cosign wire shape carried this signature."
        },
        "discovery_method": {
          "$ref": "#/$defs/DiscoveryMethod",
          "description": "How the signature was found."
        },
        "key_backend": {
          "$ref": "#/$defs/KeyBackendKind",
          "description": "What produced it: `keyless`, `file`, or a key-backend scheme."
        },
        "referrer_digest": {
          "$ref": "#/$defs/Digest",
          "description": "Digest of the referrer manifest or sidecar layer carrying it."
        },
        "certificate_identity": {
          "type": "string",
          "description": "Certificate SAN (identity) embedded in the Fulcio cert. Absent under a\nkey — a legal shape, not malformed input. Registry-served, so it is\nattacker input: see the struct note before rendering it in plain text."
        },
        "certificate_oidc_issuer": {
          "type": "string",
          "description": "Certificate OIDC issuer embedded in the Fulcio cert. Absent under a key.\nRegistry-served, so it is attacker input: see the struct note before\nrendering it in plain text."
        },
        "signed_at": {
          "type": "string",
          "description": "Rekor `integratedTime`, ISO-8601 UTC.\n\n**Certificate validity is judged against this instant, never against\nwall-clock now.** A Fulcio certificate is valid for about ten minutes;\nthe transparency-log timestamp is the only proof the signature happened\ninside that window. A keyless fixture captured today carries a\ncertificate that has already expired — a wall-clock check makes it, and\nevery keyless fixture after it, rot within the hour.\n\nAbsent when no transparency record exists (key mode without a Rekor\nupload), which is legal and must be visible rather than inferred."
        },
        "rekor_log_index": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "description": "Rekor log index, the dedup key when present."
        }
      },
      "required": [
        "signature_format",
        "discovery_method",
        "key_backend",
        "referrer_digest"
      ],
      "description": "One discovered, verified signature.\n\nThe per-signature view of what the flat [`VerificationReport`] fields\ndescribe for a single signature, plus the three things only a\nmulti-signature listing has to state: which wire shape carried it, how it\nwas found, and what produced it. The enum-valued fields reuse the library\nvocabularies verbatim (`SignatureFormat`, `DiscoveryMethod`,\n`KeyBackendKind`), so their frozen serde slugs are the wire spelling here\nand one word cannot mean two things across the two crates.\n\n# Rendering these rows in plain text requires `sanitize_for_terminal`\n\n[`certificate_identity`](Self::certificate_identity) and\n[`certificate_oidc_issuer`](Self::certificate_oidc_issuer) are read out of a\nFulcio certificate carried in a bundle a **registry** served, so they are\nattacker input by construction — the same reason\n`VerificationReport::plain_fields` routes its flat certificate fields\nthrough [`sanitize_for_terminal`]. Nothing here is exposed today: this array\nis JSON-only, and `serde_json` escapes C0 controls. The moment a row of this\nstruct reaches a plain-text table, **every** value in that row MUST go\nthrough [`sanitize_for_terminal`] first — per field, not per row, and\nincluding the typed ones, for the reason `plain_fields` states: a filter\napplied selectively has to be re-argued for every field added later, and the\nneutralization is identity on a digest, a slug and an ISO-8601 stamp.\nWithout it a SAN embedding `\\x1b]52;c;<b64>\\x07` sets the operator's\nclipboard (CWE-150)."
    },
    "DiscoveryMethod": {
      "oneOf": [
        {
          "type": "string",
          "const": "referrers_api",
          "description": "`GET /v2/<name>/referrers/<digest>` — the OCI 1.1 Referrers API."
        },
        {
          "type": "string",
          "const": "fallback_tag",
          "description": "The `sha256-<hex>` fallback referrers index a registry without the\nReferrers API gets instead."
        },
        {
          "type": "string",
          "const": "sidecar_tag",
          "description": "The cosign `sha256-<hex>.sig` / `.att` / `.sbom` sidecar tag."
        }
      ],
      "description": "Where a signature referrer was discovered.\n\nReported verbatim on `signatures[].discovery_method`, so a caller can tell an\nOCI 1.1 registry from one that only answers the fallback tag schema\nwithout inspecting the registry itself."
    },
    "VerboseVersionData": {
      "type": "object",
      "properties": {
        "version": {
          "type": "string"
        },
        "cargo_pkg_version": {
          "type": "string"
        },
        "channel": {
          "type": [
            "string",
            "null"
          ]
        },
        "commit": {
          "anyOf": [
            {
              "$ref": "#/$defs/CommitInfo"
            },
            {
              "type": "null"
            }
          ]
        },
        "build": {
          "anyOf": [
            {
              "$ref": "#/$defs/BuildInfo"
            },
            {
              "type": "null"
            }
          ]
        },
        "ci": {
          "anyOf": [
            {
              "$ref": "#/$defs/CiInfo"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "version",
        "channel",
        "commit",
        "build",
        "ci"
      ],
      "description": "Version information reported by `ocx version`.\n\n# Plain format\n\nBare version string (e.g. `0.3.1`), preserving the\npre-enrichment contract that scripts can `ocx version` and parse\nstdout as a single semver token.\n\nVerbose rendering is handled by the `VerboseVersionData` wrapper:\na multi-line summary (version, commit + dirty flag, build time,\ntarget, rustc, CI run URL). Suppressed lines for absent fields so a\nlocally-built binary doesn't show empty `ci:` rows.\n\n# JSON format\n\nThe `version` key is the always-present contract that the\n`query_installed_version` subprocess parser in\n`ocx_lib/src/package_manager/tasks/update_check.rs`\nreads when comparing the locally-installed version to the latest\nremote tag during `ocx self update`. Every other top-level key is\noptional and gated on whether the source data was available at build\ntime, so a tarball-source / no-CI build emits only `version` and a\ndev-deploy CI build emits the full schema.\n\nConcrete shape (every field except `version` is optional):\n\n```json\n{\n  \"version\":            \"0.3.2-dev+20260528143045\",\n  \"cargo_pkg_version\":  \"0.3.1\",\n  \"channel\":            \"dev\",\n  \"commit\":             { ... },\n  \"build\":              { ... },\n  \"ci\":                 { ... }\n}\n```\n\n`cargo_pkg_version` is suppressed when it would be identical to\n`version` — only meaningful for dev-deploy builds where the embedded\nversion is overridden via `__OCX_BUILD_VERSION`."
    },
    "VersionData": {
      "type": "object",
      "properties": {
        "version": {
          "type": "string"
        },
        "cargo_pkg_version": {
          "type": "string"
        },
        "channel": {
          "type": [
            "string",
            "null"
          ]
        },
        "commit": {
          "anyOf": [
            {
              "$ref": "#/$defs/CommitInfo"
            },
            {
              "type": "null"
            }
          ]
        },
        "build": {
          "anyOf": [
            {
              "$ref": "#/$defs/BuildInfo"
            },
            {
              "type": "null"
            }
          ]
        },
        "ci": {
          "anyOf": [
            {
              "$ref": "#/$defs/CiInfo"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "version",
        "channel",
        "commit",
        "build",
        "ci"
      ],
      "description": "Version information reported by `ocx version`.\n\n# Plain format\n\nBare version string (e.g. `0.3.1`), preserving the\npre-enrichment contract that scripts can `ocx version` and parse\nstdout as a single semver token.\n\nVerbose rendering is handled by the `VerboseVersionData` wrapper:\na multi-line summary (version, commit + dirty flag, build time,\ntarget, rustc, CI run URL). Suppressed lines for absent fields so a\nlocally-built binary doesn't show empty `ci:` rows.\n\n# JSON format\n\nThe `version` key is the always-present contract that the\n`query_installed_version` subprocess parser in\n`ocx_lib/src/package_manager/tasks/update_check.rs`\nreads when comparing the locally-installed version to the latest\nremote tag during `ocx self update`. Every other top-level key is\noptional and gated on whether the source data was available at build\ntime, so a tarball-source / no-CI build emits only `version` and a\ndev-deploy CI build emits the full schema.\n\nConcrete shape (every field except `version` is optional):\n\n```json\n{\n  \"version\":            \"0.3.2-dev+20260528143045\",\n  \"cargo_pkg_version\":  \"0.3.1\",\n  \"channel\":            \"dev\",\n  \"commit\":             { ... },\n  \"build\":              { ... },\n  \"ci\":                 { ... }\n}\n```\n\n`cargo_pkg_version` is suppressed when it would be identical to\n`version` — only meaningful for dev-deploy builds where the embedded\nversion is overridden via `__OCX_BUILD_VERSION`."
    },
    "WarmedPaths": {
      "type": "object",
      "properties": {
        "advisories": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/LazyAdvisoryReport"
          }
        }
      },
      "required": [
        "advisories"
      ],
      "additionalProperties": {
        "$ref": "#/$defs/WarmedPath"
      }
    },
    "WarmedPath": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string"
        },
        "kind": {
          "$ref": "#/$defs/PathKind"
        }
      },
      "required": [
        "path",
        "kind"
      ],
      "description": "One pre-warmed tool: what `ocx pull` put on disk for it, and which kind of\ndirectory that is.\n\nA tool pre-warmed eagerly yields its package root; a tool whose `lazy-mode`\nresolved to `always` yields its generated shim directory instead, because\nthat is what the run created — its package directory does not exist yet and\nnaming one would be a lie in a machine-read field."
    }
  }
}
