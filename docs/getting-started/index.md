# Getting started

1. **[Install](install.md)** — pin the SDK in a PEP 723 inline-metadata script
   or in your `pyproject.toml`.

The package currently exposes only `__version__`; there is nothing else to
call yet. The [API reference](../api/index.md) tracks what is public.
