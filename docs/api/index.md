# API reference

Auto-generated from docstrings via
[mkdocstrings](https://mkdocstrings.github.io/python/).

## Public symbols

| Symbol | Module |
|---|---|
| `__version__` | `ocx_sdk` |

Everything reachable through an underscored module path is package-private and
may change without notice.

## Conventions

- Only names exported from the top-level `ocx_sdk` package are public.
- Docstrings follow [Google style](https://google.github.io/styleguide/pyguide.html#383-functions-and-methods).
- `Raises:` sections list every exception a function may raise from the SDK's
  own hierarchy; lower-layer exceptions are wrapped and preserved on
  `__cause__`.

::: ocx_sdk
